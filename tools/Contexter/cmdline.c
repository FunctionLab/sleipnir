/*
  File autogenerated by gengetopt version 2.13.1
  generated with the following command:
  ..\gengetopt-2.13.1\Release\gengetopt.exe -N -e -i Contexter.ggo 

  The developers of gengetopt consider the fixed text that goes in all
  gengetopt output files to be in the public domain:
  we make no copyright claims on it.
*/

/* If we use autoconf.  */
#ifdef HAVE_CONFIG_H
#include "config.h"
#endif

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#include "getopt.h"

#include "cmdline.h"

static
void clear_given (struct gengetopt_args_info *args_info);
static
void clear_args (struct gengetopt_args_info *args_info);

static int
cmdline_parser_internal (int argc, char * const *argv, struct gengetopt_args_info *args_info, int override, int initialize, int check_required, const char *additional_error);

static int
cmdline_parser_required2 (struct gengetopt_args_info *args_info, const char *prog_name, const char *additional_error);

static char *
gengetopt_strdup (const char *s);

static
void clear_given (struct gengetopt_args_info *args_info)
{
  args_info->help_given = 0 ;
  args_info->version_given = 0 ;
  args_info->input_given = 0 ;
  args_info->verbosity_given = 0 ;
  args_info->dat_given = 0 ;
  args_info->database_given = 0 ;
  args_info->genes_given = 0 ;
  args_info->context_given = 0 ;
  args_info->contexts_given = 0 ;
  args_info->networks_given = 0 ;
  args_info->default_given = 0 ;
  args_info->xdsl_given = 0 ;
  args_info->minimal_in_given = 0 ;
  args_info->minimal_out_given = 0 ;
  args_info->lookup_given = 0 ;
  args_info->memmap_given = 0 ;
}

static
void clear_args (struct gengetopt_args_info *args_info)
{
  args_info->input_arg = NULL;
  args_info->verbosity_arg = 5 ;
  args_info->dat_flag = 0;
  args_info->database_arg = gengetopt_strdup (".");
  args_info->genes_arg = NULL;
  args_info->contexts_arg = NULL;
  args_info->networks_arg = gengetopt_strdup (".");
  args_info->default_arg = NULL;
  args_info->xdsl_flag = 1;
  args_info->minimal_in_flag = 0;
  args_info->minimal_out_arg = NULL;
  args_info->lookup_arg = NULL;
  args_info->memmap_flag = 0;
}

void
cmdline_parser_print_version (void)
{
  printf ("%s %s\n", CMDLINE_PARSER_PACKAGE, CMDLINE_PARSER_VERSION);
}

void
cmdline_parser_print_help (void)
{
  cmdline_parser_print_version ();
  printf("\n%s\n", "Real time Bayes net calculation from DB data");
  printf("\nUsage: Contexter [OPTIONS]...\n\n");
  printf("%s\n","  -h, --help                  Print help and exit");
  printf("%s\n","  -V, --version               Print version and exit");
  printf("%s\n","  -i, --input=filename        DAT file/Context IDs and names");
  printf("%s\n","  -v, --verbosity=INT         Message verbosity  (default=`5')");
  printf("%s\n","\nInput:");
  printf("%s\n","  -t, --dat                   Use DAT file input rather than Bayes nets  \n                                (default=off)");
  printf("%s\n","  -d, --database=directory    Database directory  (default=`.')");
  printf("%s\n","  -g, --genes=filename        Gene ID to name");
  printf("%s\n","  -e, --context=INT           Context ID to process");
  printf("%s\n","  -c, --contexts=filename     Context/gene mapping");
  printf("%s\n","\nBayes nets:");
  printf("%s\n","  -n, --networks=directory    Bayes net directory  (default=`.')");
  printf("%s\n","  -b, --default=filename      Bayes net for no context");
  printf("%s\n","  -x, --xdsl                  Use XDSL files instead of DSL  (default=on)");
  printf("%s\n","  -m, --minimal_in            Read stored contexts and minimal Bayes nets  \n                                (default=off)");
  printf("%s\n","  -M, --minimal_out=filename  Store contexts and minimal Bayes nets");
  printf("%s\n","\nOptions:");
  printf("%s\n","  -l, --lookup=STRING         Gene name to lookup");
  printf("%s\n","  -p, --memmap                Memory map input  (default=off)");
  
}

void
cmdline_parser_init (struct gengetopt_args_info *args_info)
{
  clear_given (args_info);
  clear_args (args_info);
}

void
cmdline_parser_free (struct gengetopt_args_info *args_info)
{
  
  if (args_info->input_arg)
    {
      free (args_info->input_arg); /* free previous argument */
      args_info->input_arg = 0;
    }
  if (args_info->database_arg)
    {
      free (args_info->database_arg); /* free previous argument */
      args_info->database_arg = 0;
    }
  if (args_info->genes_arg)
    {
      free (args_info->genes_arg); /* free previous argument */
      args_info->genes_arg = 0;
    }
  if (args_info->contexts_arg)
    {
      free (args_info->contexts_arg); /* free previous argument */
      args_info->contexts_arg = 0;
    }
  if (args_info->networks_arg)
    {
      free (args_info->networks_arg); /* free previous argument */
      args_info->networks_arg = 0;
    }
  if (args_info->default_arg)
    {
      free (args_info->default_arg); /* free previous argument */
      args_info->default_arg = 0;
    }
  if (args_info->minimal_out_arg)
    {
      free (args_info->minimal_out_arg); /* free previous argument */
      args_info->minimal_out_arg = 0;
    }
  if (args_info->lookup_arg)
    {
      free (args_info->lookup_arg); /* free previous argument */
      args_info->lookup_arg = 0;
    }
  
  clear_given (args_info);
}

/* gengetopt_strdup() */
/* strdup.c replacement of strdup, which is not standard */
char *
gengetopt_strdup (const char *s)
{
  char *result = NULL;
  if (!s)
    return result;

  result = (char*)malloc(strlen(s) + 1);
  if (result == (char*)0)
    return (char*)0;
  strcpy(result, s);
  return result;
}

int
cmdline_parser (int argc, char * const *argv, struct gengetopt_args_info *args_info)
{
  return cmdline_parser2 (argc, argv, args_info, 0, 1, 1);
}

int
cmdline_parser2 (int argc, char * const *argv, struct gengetopt_args_info *args_info, int override, int initialize, int check_required)
{
  int result;

  result = cmdline_parser_internal (argc, argv, args_info, override, initialize, check_required, NULL);

  return result;
}

int
cmdline_parser_required (struct gengetopt_args_info *args_info, const char *prog_name)
{
  int result = EXIT_SUCCESS;

  if (cmdline_parser_required2(args_info, prog_name, NULL) > 0)
    result = EXIT_FAILURE;

  return result;
}

int
cmdline_parser_required2 (struct gengetopt_args_info *args_info, const char *prog_name, const char *additional_error)
{
  int error = 0;

  if (! args_info->context_given)
    {
      fprintf (stderr, "%s: '--context' ('-e') option required%s\n", prog_name, (additional_error ? additional_error : ""));
      error = 1;
    }
  if (! args_info->contexts_given)
    {
      fprintf (stderr, "%s: '--contexts' ('-c') option required%s\n", prog_name, (additional_error ? additional_error : ""));
      error = 1;
    }

  return error;
}

int
cmdline_parser_internal (int argc, char * const *argv, struct gengetopt_args_info *args_info, int override, int initialize, int check_required, const char *additional_error)
{
  int c;	/* Character of the parsed option.  */

  int error = 0;
  struct gengetopt_args_info local_args_info;

  if (initialize)
    cmdline_parser_init (args_info);

  cmdline_parser_init (&local_args_info);

  optarg = 0;
  optind = 1;
  opterr = 1;
  optopt = '?';

  while (1)
    {
      int option_index = 0;
      char *stop_char;

      static struct option long_options[] = {
        { "help",	0, NULL, 'h' },
        { "version",	0, NULL, 'V' },
        { "input",	1, NULL, 'i' },
        { "verbosity",	1, NULL, 'v' },
        { "dat",	0, NULL, 't' },
        { "database",	1, NULL, 'd' },
        { "genes",	1, NULL, 'g' },
        { "context",	1, NULL, 'e' },
        { "contexts",	1, NULL, 'c' },
        { "networks",	1, NULL, 'n' },
        { "default",	1, NULL, 'b' },
        { "xdsl",	0, NULL, 'x' },
        { "minimal_in",	0, NULL, 'm' },
        { "minimal_out",	1, NULL, 'M' },
        { "lookup",	1, NULL, 'l' },
        { "memmap",	0, NULL, 'p' },
        { NULL,	0, NULL, 0 }
      };

      stop_char = 0;
      c = getopt_long (argc, argv, "hVi:v:td:g:e:c:n:b:xmM:l:p", long_options, &option_index);

      if (c == -1) break;	/* Exit from `while (1)' loop.  */

      switch (c)
        {
        case 'h':	/* Print help and exit.  */
          cmdline_parser_print_help ();
          cmdline_parser_free (&local_args_info);
          exit (EXIT_SUCCESS);

        case 'V':	/* Print version and exit.  */
          if (local_args_info.version_given)
            {
              fprintf (stderr, "%s: `--version' (`-V') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->version_given && ! override)
            continue;
          local_args_info.version_given = 1;
          args_info->version_given = 1;
          cmdline_parser_free (&local_args_info);
          return 0;

        case 'i':	/* DAT file/Context IDs and names.  */
          if (local_args_info.input_given)
            {
              fprintf (stderr, "%s: `--input' (`-i') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->input_given && ! override)
            continue;
          local_args_info.input_given = 1;
          args_info->input_given = 1;
          if (args_info->input_arg)
            free (args_info->input_arg); /* free previous string */
          args_info->input_arg = gengetopt_strdup (optarg);
          break;

        case 'v':	/* Message verbosity.  */
          if (local_args_info.verbosity_given)
            {
              fprintf (stderr, "%s: `--verbosity' (`-v') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->verbosity_given && ! override)
            continue;
          local_args_info.verbosity_given = 1;
          args_info->verbosity_given = 1;
          args_info->verbosity_arg = strtol (optarg,&stop_char,0);
          break;

        case 't':	/* Use DAT file input rather than Bayes nets.  */
          if (local_args_info.dat_given)
            {
              fprintf (stderr, "%s: `--dat' (`-t') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->dat_given && ! override)
            continue;
          local_args_info.dat_given = 1;
          args_info->dat_given = 1;
          args_info->dat_flag = !(args_info->dat_flag);
          break;

        case 'd':	/* Database directory.  */
          if (local_args_info.database_given)
            {
              fprintf (stderr, "%s: `--database' (`-d') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->database_given && ! override)
            continue;
          local_args_info.database_given = 1;
          args_info->database_given = 1;
          if (args_info->database_arg)
            free (args_info->database_arg); /* free previous string */
          args_info->database_arg = gengetopt_strdup (optarg);
          break;

        case 'g':	/* Gene ID to name.  */
          if (local_args_info.genes_given)
            {
              fprintf (stderr, "%s: `--genes' (`-g') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->genes_given && ! override)
            continue;
          local_args_info.genes_given = 1;
          args_info->genes_given = 1;
          if (args_info->genes_arg)
            free (args_info->genes_arg); /* free previous string */
          args_info->genes_arg = gengetopt_strdup (optarg);
          break;

        case 'e':	/* Context ID to process.  */
          if (local_args_info.context_given)
            {
              fprintf (stderr, "%s: `--context' (`-e') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->context_given && ! override)
            continue;
          local_args_info.context_given = 1;
          args_info->context_given = 1;
          args_info->context_arg = strtol (optarg,&stop_char,0);
          break;

        case 'c':	/* Context/gene mapping.  */
          if (local_args_info.contexts_given)
            {
              fprintf (stderr, "%s: `--contexts' (`-c') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->contexts_given && ! override)
            continue;
          local_args_info.contexts_given = 1;
          args_info->contexts_given = 1;
          if (args_info->contexts_arg)
            free (args_info->contexts_arg); /* free previous string */
          args_info->contexts_arg = gengetopt_strdup (optarg);
          break;

        case 'n':	/* Bayes net directory.  */
          if (local_args_info.networks_given)
            {
              fprintf (stderr, "%s: `--networks' (`-n') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->networks_given && ! override)
            continue;
          local_args_info.networks_given = 1;
          args_info->networks_given = 1;
          if (args_info->networks_arg)
            free (args_info->networks_arg); /* free previous string */
          args_info->networks_arg = gengetopt_strdup (optarg);
          break;

        case 'b':	/* Bayes net for no context.  */
          if (local_args_info.default_given)
            {
              fprintf (stderr, "%s: `--default' (`-b') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->default_given && ! override)
            continue;
          local_args_info.default_given = 1;
          args_info->default_given = 1;
          if (args_info->default_arg)
            free (args_info->default_arg); /* free previous string */
          args_info->default_arg = gengetopt_strdup (optarg);
          break;

        case 'x':	/* Use XDSL files instead of DSL.  */
          if (local_args_info.xdsl_given)
            {
              fprintf (stderr, "%s: `--xdsl' (`-x') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->xdsl_given && ! override)
            continue;
          local_args_info.xdsl_given = 1;
          args_info->xdsl_given = 1;
          args_info->xdsl_flag = !(args_info->xdsl_flag);
          break;

        case 'm':	/* Read stored contexts and minimal Bayes nets.  */
          if (local_args_info.minimal_in_given)
            {
              fprintf (stderr, "%s: `--minimal_in' (`-m') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->minimal_in_given && ! override)
            continue;
          local_args_info.minimal_in_given = 1;
          args_info->minimal_in_given = 1;
          args_info->minimal_in_flag = !(args_info->minimal_in_flag);
          break;

        case 'M':	/* Store contexts and minimal Bayes nets.  */
          if (local_args_info.minimal_out_given)
            {
              fprintf (stderr, "%s: `--minimal_out' (`-M') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->minimal_out_given && ! override)
            continue;
          local_args_info.minimal_out_given = 1;
          args_info->minimal_out_given = 1;
          if (args_info->minimal_out_arg)
            free (args_info->minimal_out_arg); /* free previous string */
          args_info->minimal_out_arg = gengetopt_strdup (optarg);
          break;

        case 'l':	/* Gene name to lookup.  */
          if (local_args_info.lookup_given)
            {
              fprintf (stderr, "%s: `--lookup' (`-l') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->lookup_given && ! override)
            continue;
          local_args_info.lookup_given = 1;
          args_info->lookup_given = 1;
          if (args_info->lookup_arg)
            free (args_info->lookup_arg); /* free previous string */
          args_info->lookup_arg = gengetopt_strdup (optarg);
          break;

        case 'p':	/* Memory map input.  */
          if (local_args_info.memmap_given)
            {
              fprintf (stderr, "%s: `--memmap' (`-p') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->memmap_given && ! override)
            continue;
          local_args_info.memmap_given = 1;
          args_info->memmap_given = 1;
          args_info->memmap_flag = !(args_info->memmap_flag);
          break;


        case 0:	/* Long option with no short option */
        case '?':	/* Invalid option.  */
          /* `getopt_long' already printed an error message.  */
          goto failure;

        default:	/* bug: option not considered.  */
          fprintf (stderr, "%s: option unknown: %c%s\n", CMDLINE_PARSER_PACKAGE, c, (additional_error ? additional_error : ""));
          abort ();
        } /* switch */
    } /* while */



  if (check_required)
    {
      error += cmdline_parser_required2 (args_info, argv[0], additional_error);
    }

  cmdline_parser_free (&local_args_info);

  if ( error )
    return (EXIT_FAILURE);

  return 0;

failure:
  
  cmdline_parser_free (&local_args_info);
  return (EXIT_FAILURE);
}

/*
  File autogenerated by gengetopt version 2.13.1
  generated with the following command:
  ..\gengetopt-2.13.1\Release\gengetopt.exe -u -N -e -i Data2Bnt.ggo 

  The developers of gengetopt consider the fixed text that goes in all
  gengetopt output files to be in the public domain:
  we make no copyright claims on it.
*/

/* If we use autoconf.  */
#ifdef HAVE_CONFIG_H
#include "config.h"
#endif

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#include "getopt.h"

#include "cmdline.h"

static
void clear_given (struct gengetopt_args_info *args_info);
static
void clear_args (struct gengetopt_args_info *args_info);

static int
cmdline_parser_internal (int argc, char * const *argv, struct gengetopt_args_info *args_info, int override, int initialize, int check_required, const char *additional_error);

static int
cmdline_parser_required2 (struct gengetopt_args_info *args_info, const char *prog_name, const char *additional_error);

static char *
gengetopt_strdup (const char *s);

static
void clear_given (struct gengetopt_args_info *args_info)
{
  args_info->help_given = 0 ;
  args_info->version_given = 0 ;
  args_info->features_given = 0 ;
  args_info->data_given = 0 ;
  args_info->quants_given = 0 ;
  args_info->input_given = 0 ;
  args_info->genome_given = 0 ;
  args_info->fraction_given = 0 ;
  args_info->sparse_given = 0 ;
  args_info->verbosity_given = 0 ;
  args_info->comments_given = 0 ;
  args_info->xrff_given = 0 ;
  args_info->weights_given = 0 ;
}

static
void clear_args (struct gengetopt_args_info *args_info)
{
  args_info->features_arg = NULL;
  args_info->data_arg = NULL;
  args_info->quants_arg = NULL;
  args_info->input_arg = NULL;
  args_info->genome_arg = NULL;
  args_info->fraction_arg = 1 ;
  args_info->sparse_flag = 0;
  args_info->verbosity_arg = 5 ;
  args_info->comments_flag = 0;
  args_info->xrff_flag = 0;
  args_info->weights_flag = 0;
}

void
cmdline_parser_print_version (void)
{
  printf ("%s %s\n", CMDLINE_PARSER_PACKAGE, CMDLINE_PARSER_VERSION);
}

void
cmdline_parser_print_help (void)
{
  cmdline_parser_print_version ();
  printf("\n%s\n", "BNT data file generation from data");
  printf("\nUsage: Data2Bnt [OPTIONS]... [FILES]...\n\n");
  printf("%s\n","  -h, --help               Print help and exit");
  printf("%s\n","  -V, --version            Print version and exit");
  printf("%s\n","  -f, --features=filename  List of features (nodes) and default values");
  printf("%s\n","  -d, --data=filename      Feature values for each data set");
  printf("%s\n","  -q, --quants=filename    Quantization file for membership values");
  printf("%s\n","\nOptional:");
  printf("%s\n","  -i, --input=filename     Positive gene list");
  printf("%s\n","  -g, --genome=filename    SGD features file");
  printf("%s\n","  -p, --fraction=DOUBLE    Fraction of genome to cover with default values  \n                             (default=`1')");
  printf("%s\n","  -s, --sparse             Output sparse matrix  (default=off)");
  printf("%s\n","  -v, --verbosity=INT      Message verbosity  (default=`5')");
  printf("%s\n","  -c, --comments           Include informational comments  (default=off)");
  printf("%s\n","  -x, --xrff               Generate XRFF formatted output  (default=off)");
  printf("%s\n","  -w, --weights            Weight XRFF features  (default=off)");
  
}

void
cmdline_parser_init (struct gengetopt_args_info *args_info)
{
  clear_given (args_info);
  clear_args (args_info);

  args_info->inputs = NULL;
  args_info->inputs_num = 0;
}

void
cmdline_parser_free (struct gengetopt_args_info *args_info)
{
  
  unsigned int i;
  if (args_info->features_arg)
    {
      free (args_info->features_arg); /* free previous argument */
      args_info->features_arg = 0;
    }
  if (args_info->data_arg)
    {
      free (args_info->data_arg); /* free previous argument */
      args_info->data_arg = 0;
    }
  if (args_info->quants_arg)
    {
      free (args_info->quants_arg); /* free previous argument */
      args_info->quants_arg = 0;
    }
  if (args_info->input_arg)
    {
      free (args_info->input_arg); /* free previous argument */
      args_info->input_arg = 0;
    }
  if (args_info->genome_arg)
    {
      free (args_info->genome_arg); /* free previous argument */
      args_info->genome_arg = 0;
    }
  
  for (i = 0; i < args_info->inputs_num; ++i)
    free (args_info->inputs [i]);
  
  if (args_info->inputs_num)
    free (args_info->inputs);
  
  clear_given (args_info);
}

/* gengetopt_strdup() */
/* strdup.c replacement of strdup, which is not standard */
char *
gengetopt_strdup (const char *s)
{
  char *result = NULL;
  if (!s)
    return result;

  result = (char*)malloc(strlen(s) + 1);
  if (result == (char*)0)
    return (char*)0;
  strcpy(result, s);
  return result;
}

int
cmdline_parser (int argc, char * const *argv, struct gengetopt_args_info *args_info)
{
  return cmdline_parser2 (argc, argv, args_info, 0, 1, 1);
}

int
cmdline_parser2 (int argc, char * const *argv, struct gengetopt_args_info *args_info, int override, int initialize, int check_required)
{
  int result;

  result = cmdline_parser_internal (argc, argv, args_info, override, initialize, check_required, NULL);

  return result;
}

int
cmdline_parser_required (struct gengetopt_args_info *args_info, const char *prog_name)
{
  int result = EXIT_SUCCESS;

  if (cmdline_parser_required2(args_info, prog_name, NULL) > 0)
    result = EXIT_FAILURE;

  return result;
}

int
cmdline_parser_required2 (struct gengetopt_args_info *args_info, const char *prog_name, const char *additional_error)
{
  int error = 0;

  if (! args_info->features_given)
    {
      fprintf (stderr, "%s: '--features' ('-f') option required%s\n", prog_name, (additional_error ? additional_error : ""));
      error = 1;
    }
  if (! args_info->data_given)
    {
      fprintf (stderr, "%s: '--data' ('-d') option required%s\n", prog_name, (additional_error ? additional_error : ""));
      error = 1;
    }
  if (! args_info->quants_given)
    {
      fprintf (stderr, "%s: '--quants' ('-q') option required%s\n", prog_name, (additional_error ? additional_error : ""));
      error = 1;
    }

  return error;
}

int
cmdline_parser_internal (int argc, char * const *argv, struct gengetopt_args_info *args_info, int override, int initialize, int check_required, const char *additional_error)
{
  int c;	/* Character of the parsed option.  */

  int error = 0;
  struct gengetopt_args_info local_args_info;

  if (initialize)
    cmdline_parser_init (args_info);

  cmdline_parser_init (&local_args_info);

  optarg = 0;
  optind = 1;
  opterr = 1;
  optopt = '?';

  while (1)
    {
      int option_index = 0;
      char *stop_char;

      static struct option long_options[] = {
        { "help",	0, NULL, 'h' },
        { "version",	0, NULL, 'V' },
        { "features",	1, NULL, 'f' },
        { "data",	1, NULL, 'd' },
        { "quants",	1, NULL, 'q' },
        { "input",	1, NULL, 'i' },
        { "genome",	1, NULL, 'g' },
        { "fraction",	1, NULL, 'p' },
        { "sparse",	0, NULL, 's' },
        { "verbosity",	1, NULL, 'v' },
        { "comments",	0, NULL, 'c' },
        { "xrff",	0, NULL, 'x' },
        { "weights",	0, NULL, 'w' },
        { NULL,	0, NULL, 0 }
      };

      stop_char = 0;
      c = getopt_long (argc, argv, "hVf:d:q:i:g:p:sv:cxw", long_options, &option_index);

      if (c == -1) break;	/* Exit from `while (1)' loop.  */

      switch (c)
        {
        case 'h':	/* Print help and exit.  */
          cmdline_parser_print_help ();
          cmdline_parser_free (&local_args_info);
          exit (EXIT_SUCCESS);

        case 'V':	/* Print version and exit.  */
          if (local_args_info.version_given)
            {
              fprintf (stderr, "%s: `--version' (`-V') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->version_given && ! override)
            continue;
          local_args_info.version_given = 1;
          args_info->version_given = 1;
          cmdline_parser_free (&local_args_info);
          return 0;

        case 'f':	/* List of features (nodes) and default values.  */
          if (local_args_info.features_given)
            {
              fprintf (stderr, "%s: `--features' (`-f') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->features_given && ! override)
            continue;
          local_args_info.features_given = 1;
          args_info->features_given = 1;
          if (args_info->features_arg)
            free (args_info->features_arg); /* free previous string */
          args_info->features_arg = gengetopt_strdup (optarg);
          break;

        case 'd':	/* Feature values for each data set.  */
          if (local_args_info.data_given)
            {
              fprintf (stderr, "%s: `--data' (`-d') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->data_given && ! override)
            continue;
          local_args_info.data_given = 1;
          args_info->data_given = 1;
          if (args_info->data_arg)
            free (args_info->data_arg); /* free previous string */
          args_info->data_arg = gengetopt_strdup (optarg);
          break;

        case 'q':	/* Quantization file for membership values.  */
          if (local_args_info.quants_given)
            {
              fprintf (stderr, "%s: `--quants' (`-q') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->quants_given && ! override)
            continue;
          local_args_info.quants_given = 1;
          args_info->quants_given = 1;
          if (args_info->quants_arg)
            free (args_info->quants_arg); /* free previous string */
          args_info->quants_arg = gengetopt_strdup (optarg);
          break;

        case 'i':	/* Positive gene list.  */
          if (local_args_info.input_given)
            {
              fprintf (stderr, "%s: `--input' (`-i') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->input_given && ! override)
            continue;
          local_args_info.input_given = 1;
          args_info->input_given = 1;
          if (args_info->input_arg)
            free (args_info->input_arg); /* free previous string */
          args_info->input_arg = gengetopt_strdup (optarg);
          break;

        case 'g':	/* SGD features file.  */
          if (local_args_info.genome_given)
            {
              fprintf (stderr, "%s: `--genome' (`-g') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->genome_given && ! override)
            continue;
          local_args_info.genome_given = 1;
          args_info->genome_given = 1;
          if (args_info->genome_arg)
            free (args_info->genome_arg); /* free previous string */
          args_info->genome_arg = gengetopt_strdup (optarg);
          break;

        case 'p':	/* Fraction of genome to cover with default values.  */
          if (local_args_info.fraction_given)
            {
              fprintf (stderr, "%s: `--fraction' (`-p') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->fraction_given && ! override)
            continue;
          local_args_info.fraction_given = 1;
          args_info->fraction_given = 1;
          args_info->fraction_arg = strtod (optarg, NULL);
          break;

        case 's':	/* Output sparse matrix.  */
          if (local_args_info.sparse_given)
            {
              fprintf (stderr, "%s: `--sparse' (`-s') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->sparse_given && ! override)
            continue;
          local_args_info.sparse_given = 1;
          args_info->sparse_given = 1;
          args_info->sparse_flag = !(args_info->sparse_flag);
          break;

        case 'v':	/* Message verbosity.  */
          if (local_args_info.verbosity_given)
            {
              fprintf (stderr, "%s: `--verbosity' (`-v') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->verbosity_given && ! override)
            continue;
          local_args_info.verbosity_given = 1;
          args_info->verbosity_given = 1;
          args_info->verbosity_arg = strtol (optarg,&stop_char,0);
          break;

        case 'c':	/* Include informational comments.  */
          if (local_args_info.comments_given)
            {
              fprintf (stderr, "%s: `--comments' (`-c') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->comments_given && ! override)
            continue;
          local_args_info.comments_given = 1;
          args_info->comments_given = 1;
          args_info->comments_flag = !(args_info->comments_flag);
          break;

        case 'x':	/* Generate XRFF formatted output.  */
          if (local_args_info.xrff_given)
            {
              fprintf (stderr, "%s: `--xrff' (`-x') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->xrff_given && ! override)
            continue;
          local_args_info.xrff_given = 1;
          args_info->xrff_given = 1;
          args_info->xrff_flag = !(args_info->xrff_flag);
          break;

        case 'w':	/* Weight XRFF features.  */
          if (local_args_info.weights_given)
            {
              fprintf (stderr, "%s: `--weights' (`-w') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->weights_given && ! override)
            continue;
          local_args_info.weights_given = 1;
          args_info->weights_given = 1;
          args_info->weights_flag = !(args_info->weights_flag);
          break;


        case 0:	/* Long option with no short option */
        case '?':	/* Invalid option.  */
          /* `getopt_long' already printed an error message.  */
          goto failure;

        default:	/* bug: option not considered.  */
          fprintf (stderr, "%s: option unknown: %c%s\n", CMDLINE_PARSER_PACKAGE, c, (additional_error ? additional_error : ""));
          abort ();
        } /* switch */
    } /* while */



  if (check_required)
    {
      error += cmdline_parser_required2 (args_info, argv[0], additional_error);
    }

  cmdline_parser_free (&local_args_info);

  if ( error )
    return (EXIT_FAILURE);

  if (optind < argc)
    {
      int i = 0 ;

      args_info->inputs_num = argc - optind ;
      args_info->inputs =
        (char **)(malloc ((args_info->inputs_num)*sizeof(char *))) ;
      while (optind < argc)
        args_info->inputs[ i++ ] = gengetopt_strdup (argv[optind++]) ;
    }

  return 0;

failure:
  
  cmdline_parser_free (&local_args_info);
  return (EXIT_FAILURE);
}

/*
  File autogenerated by gengetopt version 2.13.1
  generated with the following command:
  ../gengetopt-2.13.1/bin/gengetopt -C -N -e -i Explainer.ggo 

  The developers of gengetopt consider the fixed text that goes in all
  gengetopt output files to be in the public domain:
  we make no copyright claims on it.
*/

/* If we use autoconf.  */
#ifdef HAVE_CONFIG_H
#include "config.h"
#endif

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#include "getopt.h"

#include "cmdline.h"

static
void clear_given (struct gengetopt_args_info *args_info);
static
void clear_args (struct gengetopt_args_info *args_info);

static int
cmdline_parser_internal (int argc, char * const *argv, struct gengetopt_args_info *args_info, int override, int initialize, int check_required, const char *additional_error);

static int
cmdline_parser_required2 (struct gengetopt_args_info *args_info, const char *prog_name, const char *additional_error);

char *unknowns_values[] = {"exclude", "include", "only", 0} ;	/* Possible values for unknowns.  */

static char *
gengetopt_strdup (const char *s);

static
void clear_given (struct gengetopt_args_info *args_info)
{
  args_info->help_given = 0 ;
  args_info->version_given = 0 ;
  args_info->input_given = 0 ;
  args_info->answers_given = 0 ;
  args_info->invert_given = 0 ;
  args_info->genes_given = 0 ;
  args_info->genex_given = 0 ;
  args_info->genet_given = 0 ;
  args_info->count_given = 0 ;
  args_info->normalize_given = 0 ;
  args_info->positives_given = 0 ;
  args_info->features_given = 0 ;
  args_info->everything_given = 0 ;
  args_info->reverse_given = 0 ;
  args_info->go_onto_given = 0 ;
  args_info->go_anno_given = 0 ;
  args_info->unknowns_given = 0 ;
  args_info->verbosity_given = 0 ;
  args_info->memmap_given = 0 ;
  args_info->fraction_given = 0 ;
  args_info->config_given = 0 ;
}

static
void clear_args (struct gengetopt_args_info *args_info)
{
  args_info->input_arg = NULL;
  args_info->answers_arg = NULL;
  args_info->invert_flag = 0;
  args_info->genes_arg = NULL;
  args_info->genex_arg = NULL;
  args_info->genet_arg = NULL;
  args_info->count_arg = -1 ;
  args_info->normalize_flag = 0;
  args_info->positives_flag = 0;
  args_info->features_arg = NULL;
  args_info->everything_flag = 0;
  args_info->reverse_flag = 0;
  args_info->go_onto_arg = NULL;
  args_info->go_anno_arg = NULL;
  args_info->unknowns_arg = gengetopt_strdup ("exclude");
  args_info->verbosity_arg = 5 ;
  args_info->memmap_flag = 0;
  args_info->fraction_arg = 1 ;
  args_info->config_arg = gengetopt_strdup ("Explainer.ini");
}

void
cmdline_parser_print_version (void)
{
  printf ("%s %s\n", CMDLINE_PARSER_PACKAGE, CMDLINE_PARSER_VERSION);
}

void
cmdline_parser_print_help (void)
{
  cmdline_parser_print_version ();
  printf("\n%s\n", "Allows evaluation of genes contributing to a good AUC");
  printf("\nUsage: Explainer [OPTIONS]...\n\n");
  printf("%s\n","  -h, --help               Print help and exit");
  printf("%s\n","  -V, --version            Print version and exit");
  printf("%s\n","  -i, --input=filename     Correlation file");
  printf("%s\n","  -w, --answers=filename   Answer file");
  printf("%s\n","\nOptional:");
  printf("%s\n","  -t, --invert             Invert correlations to distances  (default=off)");
  printf("%s\n","  -g, --genes=filename     Gene inclusion file");
  printf("%s\n","  -G, --genex=filename     Gene exclusion file");
  printf("%s\n","  -c, --genet=filename     Term inclusion file");
  printf("%s\n","  -k, --count=INT          Number of pairs to display  (default=`-1')");
  printf("%s\n","  -n, --normalize          Normalize to the range [0,1]  (default=off)");
  printf("%s\n","  -p, --positives          Include only positive pairs  (default=off)");
  printf("%s\n","  -f, --features=filename  SGD gene features");
  printf("%s\n","  -e, --everything         Include pairs without answers  (default=off)");
  printf("%s\n","  -r, --reverse            Reverse sort order  (default=off)");
  printf("%s\n","  -o, --go_onto=filename   GO ontology");
  printf("%s\n","  -a, --go_anno=filename   GO annotations");
  printf("%s\n","  -u, --unknowns=STRING    Treatment of unknown genes  (possible \n                             values=\"exclude\", \"include\", \"only\" \n                             default=`exclude')");
  printf("%s\n","  -v, --verbosity=INT      Message verbosity  (default=`5')");
  printf("%s\n","  -m, --memmap             Memory map input files  (default=off)");
  printf("%s\n","  -x, --fraction=DOUBLE    Random fraction of results to calculate  \n                             (default=`1')");
  printf("%s\n","  -C, --config=filename    Command line config file  (default=`Explainer.ini')");
  
}

void
cmdline_parser_init (struct gengetopt_args_info *args_info)
{
  clear_given (args_info);
  clear_args (args_info);
}

void
cmdline_parser_free (struct gengetopt_args_info *args_info)
{
  
  if (args_info->input_arg)
    {
      free (args_info->input_arg); /* free previous argument */
      args_info->input_arg = 0;
    }
  if (args_info->answers_arg)
    {
      free (args_info->answers_arg); /* free previous argument */
      args_info->answers_arg = 0;
    }
  if (args_info->genes_arg)
    {
      free (args_info->genes_arg); /* free previous argument */
      args_info->genes_arg = 0;
    }
  if (args_info->genex_arg)
    {
      free (args_info->genex_arg); /* free previous argument */
      args_info->genex_arg = 0;
    }
  if (args_info->genet_arg)
    {
      free (args_info->genet_arg); /* free previous argument */
      args_info->genet_arg = 0;
    }
  if (args_info->features_arg)
    {
      free (args_info->features_arg); /* free previous argument */
      args_info->features_arg = 0;
    }
  if (args_info->go_onto_arg)
    {
      free (args_info->go_onto_arg); /* free previous argument */
      args_info->go_onto_arg = 0;
    }
  if (args_info->go_anno_arg)
    {
      free (args_info->go_anno_arg); /* free previous argument */
      args_info->go_anno_arg = 0;
    }
  if (args_info->unknowns_arg)
    {
      free (args_info->unknowns_arg); /* free previous argument */
      args_info->unknowns_arg = 0;
    }
  if (args_info->config_arg)
    {
      free (args_info->config_arg); /* free previous argument */
      args_info->config_arg = 0;
    }
  
  clear_given (args_info);
}

static int
check_possible_values(const char *val, char *values[])
{
  int i, found;
  size_t len;

  if (!val)   /* otherwise strlen() crashes below */
    return 0; /* NULL means no argument for the option */

  for (found = i = 0, len = strlen(val); values[i]; ++i)
    {
      if (strncmp(val, values[i], len) == 0)
        {
          found++;
          if (strlen(values[i]) == len)
            return 1; /* exact macth no need to check more */
        }
    }

  return found; /* return how many values are matched */
}

/* gengetopt_strdup() */
/* strdup.c replacement of strdup, which is not standard */
char *
gengetopt_strdup (const char *s)
{
  char *result = NULL;
  if (!s)
    return result;

  result = (char*)malloc(strlen(s) + 1);
  if (result == (char*)0)
    return (char*)0;
  strcpy(result, s);
  return result;
}

int
cmdline_parser (int argc, char * const *argv, struct gengetopt_args_info *args_info)
{
  return cmdline_parser2 (argc, argv, args_info, 0, 1, 1);
}

int
cmdline_parser2 (int argc, char * const *argv, struct gengetopt_args_info *args_info, int override, int initialize, int check_required)
{
  int result;

  result = cmdline_parser_internal (argc, argv, args_info, override, initialize, check_required, NULL);

  return result;
}

int
cmdline_parser_required (struct gengetopt_args_info *args_info, const char *prog_name)
{
  int result = EXIT_SUCCESS;

  if (cmdline_parser_required2(args_info, prog_name, NULL) > 0)
    result = EXIT_FAILURE;

  return result;
}

int
cmdline_parser_required2 (struct gengetopt_args_info *args_info, const char *prog_name, const char *additional_error)
{
  int error = 0;

  if (! args_info->input_given)
    {
      fprintf (stderr, "%s: '--input' ('-i') option required%s\n", prog_name, (additional_error ? additional_error : ""));
      error = 1;
    }
  if (! args_info->answers_given)
    {
      fprintf (stderr, "%s: '--answers' ('-w') option required%s\n", prog_name, (additional_error ? additional_error : ""));
      error = 1;
    }

  return error;
}

int
cmdline_parser_internal (int argc, char * const *argv, struct gengetopt_args_info *args_info, int override, int initialize, int check_required, const char *additional_error)
{
  int c;	/* Character of the parsed option.  */

  int error = 0;
  struct gengetopt_args_info local_args_info;

  if (initialize)
    cmdline_parser_init (args_info);

  cmdline_parser_init (&local_args_info);

  optarg = 0;
  optind = 1;
  opterr = 1;
  optopt = '?';

  while (1)
    {
      int found = 0;
      int option_index = 0;
      char *stop_char;

      static struct option long_options[] = {
        { "help",	0, NULL, 'h' },
        { "version",	0, NULL, 'V' },
        { "input",	1, NULL, 'i' },
        { "answers",	1, NULL, 'w' },
        { "invert",	0, NULL, 't' },
        { "genes",	1, NULL, 'g' },
        { "genex",	1, NULL, 'G' },
        { "genet",	1, NULL, 'c' },
        { "count",	1, NULL, 'k' },
        { "normalize",	0, NULL, 'n' },
        { "positives",	0, NULL, 'p' },
        { "features",	1, NULL, 'f' },
        { "everything",	0, NULL, 'e' },
        { "reverse",	0, NULL, 'r' },
        { "go_onto",	1, NULL, 'o' },
        { "go_anno",	1, NULL, 'a' },
        { "unknowns",	1, NULL, 'u' },
        { "verbosity",	1, NULL, 'v' },
        { "memmap",	0, NULL, 'm' },
        { "fraction",	1, NULL, 'x' },
        { "config",	1, NULL, 'C' },
        { NULL,	0, NULL, 0 }
      };

      stop_char = 0;
      c = getopt_long (argc, argv, "hVi:w:tg:G:c:k:npf:ero:a:u:v:mx:C:", long_options, &option_index);

      if (c == -1) break;	/* Exit from `while (1)' loop.  */

      switch (c)
        {
        case 'h':	/* Print help and exit.  */
          cmdline_parser_print_help ();
          cmdline_parser_free (&local_args_info);
          exit (EXIT_SUCCESS);

        case 'V':	/* Print version and exit.  */
          if (local_args_info.version_given)
            {
              fprintf (stderr, "%s: `--version' (`-V') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->version_given && ! override)
            continue;
          local_args_info.version_given = 1;
          args_info->version_given = 1;
          cmdline_parser_free (&local_args_info);
          return 0;

        case 'i':	/* Correlation file.  */
          if (local_args_info.input_given)
            {
              fprintf (stderr, "%s: `--input' (`-i') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->input_given && ! override)
            continue;
          local_args_info.input_given = 1;
          args_info->input_given = 1;
          if (args_info->input_arg)
            free (args_info->input_arg); /* free previous string */
          args_info->input_arg = gengetopt_strdup (optarg);
          break;

        case 'w':	/* Answer file.  */
          if (local_args_info.answers_given)
            {
              fprintf (stderr, "%s: `--answers' (`-w') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->answers_given && ! override)
            continue;
          local_args_info.answers_given = 1;
          args_info->answers_given = 1;
          if (args_info->answers_arg)
            free (args_info->answers_arg); /* free previous string */
          args_info->answers_arg = gengetopt_strdup (optarg);
          break;

        case 't':	/* Invert correlations to distances.  */
          if (local_args_info.invert_given)
            {
              fprintf (stderr, "%s: `--invert' (`-t') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->invert_given && ! override)
            continue;
          local_args_info.invert_given = 1;
          args_info->invert_given = 1;
          args_info->invert_flag = !(args_info->invert_flag);
          break;

        case 'g':	/* Gene inclusion file.  */
          if (local_args_info.genes_given)
            {
              fprintf (stderr, "%s: `--genes' (`-g') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->genes_given && ! override)
            continue;
          local_args_info.genes_given = 1;
          args_info->genes_given = 1;
          if (args_info->genes_arg)
            free (args_info->genes_arg); /* free previous string */
          args_info->genes_arg = gengetopt_strdup (optarg);
          break;

        case 'G':	/* Gene exclusion file.  */
          if (local_args_info.genex_given)
            {
              fprintf (stderr, "%s: `--genex' (`-G') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->genex_given && ! override)
            continue;
          local_args_info.genex_given = 1;
          args_info->genex_given = 1;
          if (args_info->genex_arg)
            free (args_info->genex_arg); /* free previous string */
          args_info->genex_arg = gengetopt_strdup (optarg);
          break;

        case 'c':	/* Term inclusion file.  */
          if (local_args_info.genet_given)
            {
              fprintf (stderr, "%s: `--genet' (`-c') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->genet_given && ! override)
            continue;
          local_args_info.genet_given = 1;
          args_info->genet_given = 1;
          if (args_info->genet_arg)
            free (args_info->genet_arg); /* free previous string */
          args_info->genet_arg = gengetopt_strdup (optarg);
          break;

        case 'k':	/* Number of pairs to display.  */
          if (local_args_info.count_given)
            {
              fprintf (stderr, "%s: `--count' (`-k') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->count_given && ! override)
            continue;
          local_args_info.count_given = 1;
          args_info->count_given = 1;
          args_info->count_arg = strtol (optarg,&stop_char,0);
          break;

        case 'n':	/* Normalize to the range [0,1].  */
          if (local_args_info.normalize_given)
            {
              fprintf (stderr, "%s: `--normalize' (`-n') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->normalize_given && ! override)
            continue;
          local_args_info.normalize_given = 1;
          args_info->normalize_given = 1;
          args_info->normalize_flag = !(args_info->normalize_flag);
          break;

        case 'p':	/* Include only positive pairs.  */
          if (local_args_info.positives_given)
            {
              fprintf (stderr, "%s: `--positives' (`-p') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->positives_given && ! override)
            continue;
          local_args_info.positives_given = 1;
          args_info->positives_given = 1;
          args_info->positives_flag = !(args_info->positives_flag);
          break;

        case 'f':	/* SGD gene features.  */
          if (local_args_info.features_given)
            {
              fprintf (stderr, "%s: `--features' (`-f') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->features_given && ! override)
            continue;
          local_args_info.features_given = 1;
          args_info->features_given = 1;
          if (args_info->features_arg)
            free (args_info->features_arg); /* free previous string */
          args_info->features_arg = gengetopt_strdup (optarg);
          break;

        case 'e':	/* Include pairs without answers.  */
          if (local_args_info.everything_given)
            {
              fprintf (stderr, "%s: `--everything' (`-e') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->everything_given && ! override)
            continue;
          local_args_info.everything_given = 1;
          args_info->everything_given = 1;
          args_info->everything_flag = !(args_info->everything_flag);
          break;

        case 'r':	/* Reverse sort order.  */
          if (local_args_info.reverse_given)
            {
              fprintf (stderr, "%s: `--reverse' (`-r') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->reverse_given && ! override)
            continue;
          local_args_info.reverse_given = 1;
          args_info->reverse_given = 1;
          args_info->reverse_flag = !(args_info->reverse_flag);
          break;

        case 'o':	/* GO ontology.  */
          if (local_args_info.go_onto_given)
            {
              fprintf (stderr, "%s: `--go_onto' (`-o') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->go_onto_given && ! override)
            continue;
          local_args_info.go_onto_given = 1;
          args_info->go_onto_given = 1;
          if (args_info->go_onto_arg)
            free (args_info->go_onto_arg); /* free previous string */
          args_info->go_onto_arg = gengetopt_strdup (optarg);
          break;

        case 'a':	/* GO annotations.  */
          if (local_args_info.go_anno_given)
            {
              fprintf (stderr, "%s: `--go_anno' (`-a') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->go_anno_given && ! override)
            continue;
          local_args_info.go_anno_given = 1;
          args_info->go_anno_given = 1;
          if (args_info->go_anno_arg)
            free (args_info->go_anno_arg); /* free previous string */
          args_info->go_anno_arg = gengetopt_strdup (optarg);
          break;

        case 'u':	/* Treatment of unknown genes.  */
          if (local_args_info.unknowns_given)
            {
              fprintf (stderr, "%s: `--unknowns' (`-u') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if ((found = check_possible_values(optarg, unknowns_values)) != 1)
            {
              fprintf (stderr, "%s: %s argument, \"%s\", for option `--unknowns' (`-u')%s\n", argv[0], found ? "ambiguous" : "invalid", optarg, (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->unknowns_given && ! override)
            continue;
          local_args_info.unknowns_given = 1;
          args_info->unknowns_given = 1;
          if (args_info->unknowns_arg)
            free (args_info->unknowns_arg); /* free previous string */
          args_info->unknowns_arg = gengetopt_strdup (optarg);
          break;

        case 'v':	/* Message verbosity.  */
          if (local_args_info.verbosity_given)
            {
              fprintf (stderr, "%s: `--verbosity' (`-v') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->verbosity_given && ! override)
            continue;
          local_args_info.verbosity_given = 1;
          args_info->verbosity_given = 1;
          args_info->verbosity_arg = strtol (optarg,&stop_char,0);
          break;

        case 'm':	/* Memory map input files.  */
          if (local_args_info.memmap_given)
            {
              fprintf (stderr, "%s: `--memmap' (`-m') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->memmap_given && ! override)
            continue;
          local_args_info.memmap_given = 1;
          args_info->memmap_given = 1;
          args_info->memmap_flag = !(args_info->memmap_flag);
          break;

        case 'x':	/* Random fraction of results to calculate.  */
          if (local_args_info.fraction_given)
            {
              fprintf (stderr, "%s: `--fraction' (`-x') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->fraction_given && ! override)
            continue;
          local_args_info.fraction_given = 1;
          args_info->fraction_given = 1;
          args_info->fraction_arg = strtod (optarg, NULL);
          break;

        case 'C':	/* Command line config file.  */
          if (local_args_info.config_given)
            {
              fprintf (stderr, "%s: `--config' (`-C') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->config_given && ! override)
            continue;
          local_args_info.config_given = 1;
          args_info->config_given = 1;
          if (args_info->config_arg)
            free (args_info->config_arg); /* free previous string */
          args_info->config_arg = gengetopt_strdup (optarg);
          break;


        case 0:	/* Long option with no short option */
        case '?':	/* Invalid option.  */
          /* `getopt_long' already printed an error message.  */
          goto failure;

        default:	/* bug: option not considered.  */
          fprintf (stderr, "%s: option unknown: %c%s\n", CMDLINE_PARSER_PACKAGE, c, (additional_error ? additional_error : ""));
          abort ();
        } /* switch */
    } /* while */



  if (check_required)
    {
      error += cmdline_parser_required2 (args_info, argv[0], additional_error);
    }

  cmdline_parser_free (&local_args_info);

  if ( error )
    return (EXIT_FAILURE);

  return 0;

failure:
  
  cmdline_parser_free (&local_args_info);
  return (EXIT_FAILURE);
}

#define CONFIGPARSERBUFSIZE 256
#define ADDITIONAL_ERROR " in configuration file "

#define CONF_FILE_LINES 256
#define CONF_FILE_STRING (CONFIGPARSERBUFSIZE+3)
/* 3 is for "--" and "=" */

typedef char buffer_line_t[CONF_FILE_LINES];

buffer_line_t
my_argv[CONF_FILE_STRING];

int
cmdline_parser_configfile (char * const filename, struct gengetopt_args_info *args_info, int override, int initialize, int check_required)
{
  FILE* file;
  char linebuf[CONFIGPARSERBUFSIZE];
  int line_num = 0;
  int i, result, equal;
  char *fopt, *farg;
  char *str_index;
  size_t len, next_token;
  char delimiter;
  int my_argc = 0;
  char **my_argv_arg;
  char *additional_error;

  strcpy (my_argv[0], CMDLINE_PARSER_PACKAGE);
  for (i = 1; i < CONF_FILE_LINES; ++i)
    my_argv[i][0] = '\0';

  if ((file = fopen(filename, "r")) == NULL)
    {
      fprintf (stderr, "%s: Error opening configuration file '%s'\n",
               CMDLINE_PARSER_PACKAGE, filename);
      result = EXIT_FAILURE;
      goto conf_failure;
    }

  while ((fgets(linebuf, CONFIGPARSERBUFSIZE, file)) != NULL)
    {
      ++line_num;
      len = strlen(linebuf);
      if (len > (CONF_FILE_STRING-1))
        {
          fprintf (stderr, "%s:%s:%d: Line too long in configuration file\n",
                   CMDLINE_PARSER_PACKAGE, filename, line_num);
          result = EXIT_FAILURE;
          goto conf_failure;
        }

      /* find first non-whitespace character in the line */
      next_token = strspn ( linebuf, " \t\r\n");
      str_index  = linebuf + next_token;

      if ( str_index[0] == '\0' || str_index[0] == '#')
        continue; /* empty line or comment line is skipped */

      fopt = str_index;

      /* truncate fopt at the end of the first non-valid character */
      next_token = strcspn (fopt, " \t\r\n=");

      if (fopt[next_token] == '\0') /* the line is over */
        {
          farg  = NULL;
          equal = 0;
          goto noarg;
        }

      /* remember if equal sign is present */
      equal = (fopt[next_token] == '=');
      fopt[next_token++] = '\0';

      /* advance pointers to the next token after the end of fopt */
      next_token += strspn (fopt + next_token, " \t\r\n");
      /* check for the presence of equal sign, and if so, skip it */
      if ( !equal )
        if ((equal = (fopt[next_token] == '=')))
          {
            next_token++;
            next_token += strspn (fopt + next_token, " \t\r\n");
          }
      str_index  += next_token;

      /* find argument */
      farg = str_index;
      if ( farg[0] == '\"' || farg[0] == '\'' )
        { /* quoted argument */
          str_index = strchr (++farg, str_index[0] ); /* skip opening quote */
          if (! str_index)
            {
              fprintf
                (stderr,
                 "%s:%s:%d: unterminated string in configuration file\n",
                 CMDLINE_PARSER_PACKAGE, filename, line_num);
              result = EXIT_FAILURE;
              goto conf_failure;
            }
        }
      else
        { /* read up the remaining part up to a delimiter */
          next_token = strcspn (farg, " \t\r\n#\'\"");
          str_index += next_token;
        }

      /* truncate farg at the delimiter and store it for further check */
      delimiter = *str_index, *str_index++ = '\0';

      /* everything but comment is illegal at the end of line */
      if (delimiter != '\0' && delimiter != '#')
        {
          str_index += strspn(str_index, " \t\r\n");
          if (*str_index != '\0' && *str_index != '#')
            {
              fprintf
                (stderr,
                 "%s:%s:%d: malformed string in configuration file\n",
                 CMDLINE_PARSER_PACKAGE, filename, line_num);
              result = EXIT_FAILURE;
              goto conf_failure;
            }
        }

    noarg:
      ++my_argc;
      len = strlen(fopt);

      strcat (my_argv[my_argc], len > 1 ? "--" : "-");
      strcat (my_argv[my_argc], fopt);
      if (len > 1 && ((farg &&*farg) || equal))
            strcat (my_argv[my_argc], "=");
      if (farg && *farg)
          strcat (my_argv[my_argc], farg);
    } /* while */

  ++my_argc; /* for program name */
  my_argv_arg = (char **) malloc((my_argc+1) * sizeof(char *));
  for (i = 0; i < my_argc; ++i)
    my_argv_arg[i] = my_argv[i];
  my_argv_arg[my_argc] = 0;

  additional_error = (char *)malloc(strlen(filename) + strlen(ADDITIONAL_ERROR) + 1);
  strcpy (additional_error, ADDITIONAL_ERROR);
  strcat (additional_error, filename);
  result =
    cmdline_parser_internal (my_argc, my_argv_arg, args_info, override, initialize, check_required, additional_error);

  free (additional_error);
  free (my_argv_arg);

conf_failure:
  if (file)
    fclose(file);
  return result;
}

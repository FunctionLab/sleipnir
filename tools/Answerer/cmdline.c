/*
  File autogenerated by gengetopt version 2.13.1
  generated with the following command:
  ..\gengetopt-2.13.1\Release\gengetopt.exe -N -e -i Answerer.ggo 

  The developers of gengetopt consider the fixed text that goes in all
  gengetopt output files to be in the public domain:
  we make no copyright claims on it.
*/

/* If we use autoconf.  */
#ifdef HAVE_CONFIG_H
#include "config.h"
#endif

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#include "getopt.h"

#include "cmdline.h"

static
void clear_given (struct gengetopt_args_info *args_info);
static
void clear_args (struct gengetopt_args_info *args_info);

static int
cmdline_parser_internal (int argc, char * const *argv, struct gengetopt_args_info *args_info, int override, int initialize, int check_required, const char *additional_error);


static char *
gengetopt_strdup (const char *s);

static
void clear_given (struct gengetopt_args_info *args_info)
{
  args_info->help_given = 0 ;
  args_info->version_given = 0 ;
  args_info->positives_given = 0 ;
  args_info->input_given = 0 ;
  args_info->negatives_given = 0 ;
  args_info->interactions_given = 0 ;
  args_info->genome_given = 0 ;
  args_info->test_given = 0 ;
  args_info->output_given = 0 ;
  args_info->random_given = 0 ;
  args_info->overlap_given = 0 ;
  args_info->verbosity_given = 0 ;
  args_info->Negatives_group_counter = 0 ;
  args_info->Positives_group_counter = 0 ;
}

static
void clear_args (struct gengetopt_args_info *args_info)
{
  args_info->positives_arg = NULL;
  args_info->input_arg = NULL;
  args_info->negatives_arg = NULL;
  args_info->genome_arg = NULL;
  args_info->test_arg = 0 ;
  args_info->output_arg = NULL;
  args_info->random_arg = 0 ;
  args_info->overlap_arg = 0 ;
  args_info->verbosity_arg = 5 ;
}

void
cmdline_parser_print_version (void)
{
  printf ("%s %s\n", CMDLINE_PARSER_PACKAGE, CMDLINE_PARSER_VERSION);
}

void
cmdline_parser_print_help (void)
{
  cmdline_parser_print_version ();
  printf("\n%s\n", "Generates an answer file given positives, negatives, and a genome");
  printf("\nUsage: Answerer [OPTIONS]...\n\n");
  printf("%s\n","  -h, --help                 Print help and exit");
  printf("%s\n","  -V, --version              Print version and exit");
  printf("%s\n","\n Group: Positives");
  printf("%s\n","  -p, --positives=directory  Directory containing related gene lists");
  printf("%s\n","  -i, --input=filename       Pre-existing positive DAT file");
  printf("%s\n","\n Group: Negatives");
  printf("%s\n","  -n, --negatives=directory  Directory containing minimally related gene lists");
  printf("%s\n","  -x, --interactions=INT     Expected interactions per gene");
  printf("%s\n","\nOptional:");
  printf("%s\n","  -g, --genome=filename      List of all genes to be considered");
  printf("%s\n","  -t, --test=DOUBLE          Fraction of genes to randomly select for testing  \n                               (default=`0')");
  printf("%s\n","  -o, --output=filename      Output dab file");
  printf("%s\n","  -r, --random=INT           Seed random generator  (default=`0')");
  printf("%s\n","  -l, --overlap=DOUBLE       P-value cutoff for negative term overlap  \n                               (default=`0')");
  printf("%s\n","  -v, --verbosity=INT        Message verbosity  (default=`5')");
  
}

void
cmdline_parser_init (struct gengetopt_args_info *args_info)
{
  clear_given (args_info);
  clear_args (args_info);
}

void
cmdline_parser_free (struct gengetopt_args_info *args_info)
{
  
  if (args_info->positives_arg)
    {
      free (args_info->positives_arg); /* free previous argument */
      args_info->positives_arg = 0;
    }
  if (args_info->input_arg)
    {
      free (args_info->input_arg); /* free previous argument */
      args_info->input_arg = 0;
    }
  if (args_info->negatives_arg)
    {
      free (args_info->negatives_arg); /* free previous argument */
      args_info->negatives_arg = 0;
    }
  if (args_info->genome_arg)
    {
      free (args_info->genome_arg); /* free previous argument */
      args_info->genome_arg = 0;
    }
  if (args_info->output_arg)
    {
      free (args_info->output_arg); /* free previous argument */
      args_info->output_arg = 0;
    }
  
  clear_given (args_info);
}

/* gengetopt_strdup() */
/* strdup.c replacement of strdup, which is not standard */
char *
gengetopt_strdup (const char *s)
{
  char *result = NULL;
  if (!s)
    return result;

  result = (char*)malloc(strlen(s) + 1);
  if (result == (char*)0)
    return (char*)0;
  strcpy(result, s);
  return result;
}

static void
reset_group_Negatives(struct gengetopt_args_info *args_info);

static void
reset_group_Negatives(struct gengetopt_args_info *args_info)
{
  if (! args_info->Negatives_group_counter)
    return;
  
  args_info->negatives_given = 0 ;
  if (args_info->negatives_arg)
    {
      free (args_info->negatives_arg); /* free previous argument */
      args_info->negatives_arg = 0;
    }
  args_info->interactions_given = 0 ;

  args_info->Negatives_group_counter = 0;
}

static void
reset_group_Positives(struct gengetopt_args_info *args_info);

static void
reset_group_Positives(struct gengetopt_args_info *args_info)
{
  if (! args_info->Positives_group_counter)
    return;
  
  args_info->positives_given = 0 ;
  if (args_info->positives_arg)
    {
      free (args_info->positives_arg); /* free previous argument */
      args_info->positives_arg = 0;
    }
  args_info->input_given = 0 ;
  if (args_info->input_arg)
    {
      free (args_info->input_arg); /* free previous argument */
      args_info->input_arg = 0;
    }

  args_info->Positives_group_counter = 0;
}

int
cmdline_parser (int argc, char * const *argv, struct gengetopt_args_info *args_info)
{
  return cmdline_parser2 (argc, argv, args_info, 0, 1, 1);
}

int
cmdline_parser2 (int argc, char * const *argv, struct gengetopt_args_info *args_info, int override, int initialize, int check_required)
{
  int result;

  result = cmdline_parser_internal (argc, argv, args_info, override, initialize, check_required, NULL);

  return result;
}

int
cmdline_parser_required (struct gengetopt_args_info *args_info, const char *prog_name)
{
  return EXIT_SUCCESS;
}

int
cmdline_parser_internal (int argc, char * const *argv, struct gengetopt_args_info *args_info, int override, int initialize, int check_required, const char *additional_error)
{
  int c;	/* Character of the parsed option.  */

  int error = 0;
  struct gengetopt_args_info local_args_info;

  if (initialize)
    cmdline_parser_init (args_info);

  cmdline_parser_init (&local_args_info);

  optarg = 0;
  optind = 1;
  opterr = 1;
  optopt = '?';

  while (1)
    {
      int option_index = 0;
      char *stop_char;

      static struct option long_options[] = {
        { "help",	0, NULL, 'h' },
        { "version",	0, NULL, 'V' },
        { "positives",	1, NULL, 'p' },
        { "input",	1, NULL, 'i' },
        { "negatives",	1, NULL, 'n' },
        { "interactions",	1, NULL, 'x' },
        { "genome",	1, NULL, 'g' },
        { "test",	1, NULL, 't' },
        { "output",	1, NULL, 'o' },
        { "random",	1, NULL, 'r' },
        { "overlap",	1, NULL, 'l' },
        { "verbosity",	1, NULL, 'v' },
        { NULL,	0, NULL, 0 }
      };

      stop_char = 0;
      c = getopt_long (argc, argv, "hVp:i:n:x:g:t:o:r:l:v:", long_options, &option_index);

      if (c == -1) break;	/* Exit from `while (1)' loop.  */

      switch (c)
        {
        case 'h':	/* Print help and exit.  */
          cmdline_parser_print_help ();
          cmdline_parser_free (&local_args_info);
          exit (EXIT_SUCCESS);

        case 'V':	/* Print version and exit.  */
          if (local_args_info.version_given)
            {
              fprintf (stderr, "%s: `--version' (`-V') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->version_given && ! override)
            continue;
          local_args_info.version_given = 1;
          args_info->version_given = 1;
          cmdline_parser_free (&local_args_info);
          return 0;

        case 'p':	/* Directory containing related gene lists.  */
          if (local_args_info.positives_given)
            {
              fprintf (stderr, "%s: `--positives' (`-p') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->positives_given && ! override)
            continue;
          local_args_info.positives_given = 1;
          args_info->positives_given = 1;
          if (args_info->Positives_group_counter && override)
            reset_group_Positives (args_info);
          args_info->Positives_group_counter += 1;
          if (args_info->positives_arg)
            free (args_info->positives_arg); /* free previous string */
          args_info->positives_arg = gengetopt_strdup (optarg);
          break;

        case 'i':	/* Pre-existing positive DAT file.  */
          if (local_args_info.input_given)
            {
              fprintf (stderr, "%s: `--input' (`-i') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->input_given && ! override)
            continue;
          local_args_info.input_given = 1;
          args_info->input_given = 1;
          if (args_info->Positives_group_counter && override)
            reset_group_Positives (args_info);
          args_info->Positives_group_counter += 1;
          if (args_info->input_arg)
            free (args_info->input_arg); /* free previous string */
          args_info->input_arg = gengetopt_strdup (optarg);
          break;

        case 'n':	/* Directory containing minimally related gene lists.  */
          if (local_args_info.negatives_given)
            {
              fprintf (stderr, "%s: `--negatives' (`-n') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->negatives_given && ! override)
            continue;
          local_args_info.negatives_given = 1;
          args_info->negatives_given = 1;
          if (args_info->Negatives_group_counter && override)
            reset_group_Negatives (args_info);
          args_info->Negatives_group_counter += 1;
          if (args_info->negatives_arg)
            free (args_info->negatives_arg); /* free previous string */
          args_info->negatives_arg = gengetopt_strdup (optarg);
          break;

        case 'x':	/* Expected interactions per gene.  */
          if (local_args_info.interactions_given)
            {
              fprintf (stderr, "%s: `--interactions' (`-x') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->interactions_given && ! override)
            continue;
          local_args_info.interactions_given = 1;
          args_info->interactions_given = 1;
          if (args_info->Negatives_group_counter && override)
            reset_group_Negatives (args_info);
          args_info->Negatives_group_counter += 1;
          args_info->interactions_arg = strtol (optarg,&stop_char,0);
          break;

        case 'g':	/* List of all genes to be considered.  */
          if (local_args_info.genome_given)
            {
              fprintf (stderr, "%s: `--genome' (`-g') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->genome_given && ! override)
            continue;
          local_args_info.genome_given = 1;
          args_info->genome_given = 1;
          if (args_info->genome_arg)
            free (args_info->genome_arg); /* free previous string */
          args_info->genome_arg = gengetopt_strdup (optarg);
          break;

        case 't':	/* Fraction of genes to randomly select for testing.  */
          if (local_args_info.test_given)
            {
              fprintf (stderr, "%s: `--test' (`-t') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->test_given && ! override)
            continue;
          local_args_info.test_given = 1;
          args_info->test_given = 1;
          args_info->test_arg = strtod (optarg, NULL);
          break;

        case 'o':	/* Output dab file.  */
          if (local_args_info.output_given)
            {
              fprintf (stderr, "%s: `--output' (`-o') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->output_given && ! override)
            continue;
          local_args_info.output_given = 1;
          args_info->output_given = 1;
          if (args_info->output_arg)
            free (args_info->output_arg); /* free previous string */
          args_info->output_arg = gengetopt_strdup (optarg);
          break;

        case 'r':	/* Seed random generator.  */
          if (local_args_info.random_given)
            {
              fprintf (stderr, "%s: `--random' (`-r') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->random_given && ! override)
            continue;
          local_args_info.random_given = 1;
          args_info->random_given = 1;
          args_info->random_arg = strtol (optarg,&stop_char,0);
          break;

        case 'l':	/* P-value cutoff for negative term overlap.  */
          if (local_args_info.overlap_given)
            {
              fprintf (stderr, "%s: `--overlap' (`-l') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->overlap_given && ! override)
            continue;
          local_args_info.overlap_given = 1;
          args_info->overlap_given = 1;
          args_info->overlap_arg = strtod (optarg, NULL);
          break;

        case 'v':	/* Message verbosity.  */
          if (local_args_info.verbosity_given)
            {
              fprintf (stderr, "%s: `--verbosity' (`-v') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->verbosity_given && ! override)
            continue;
          local_args_info.verbosity_given = 1;
          args_info->verbosity_given = 1;
          args_info->verbosity_arg = strtol (optarg,&stop_char,0);
          break;


        case 0:	/* Long option with no short option */
        case '?':	/* Invalid option.  */
          /* `getopt_long' already printed an error message.  */
          goto failure;

        default:	/* bug: option not considered.  */
          fprintf (stderr, "%s: option unknown: %c%s\n", CMDLINE_PARSER_PACKAGE, c, (additional_error ? additional_error : ""));
          abort ();
        } /* switch */
    } /* while */

  if ( args_info->Negatives_group_counter > 1)
    {
      fprintf (stderr, "%s: %d options of group Negatives were given. At most one is required.%s\n", argv[0], args_info->Negatives_group_counter, (additional_error ? additional_error : ""));
      error = 1;
    }
  
  if ( args_info->Positives_group_counter != 1)
    {
      fprintf (stderr, "%s: %d options of group Positives were given. One is required.%s\n", argv[0], args_info->Positives_group_counter, (additional_error ? additional_error : ""));
      error = 1;
    }
  



  cmdline_parser_free (&local_args_info);

  if ( error )
    return (EXIT_FAILURE);

  return 0;

failure:
  
  cmdline_parser_free (&local_args_info);
  return (EXIT_FAILURE);
}

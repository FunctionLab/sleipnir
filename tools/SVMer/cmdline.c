/*
  File autogenerated by gengetopt version 2.13.1
  generated with the following command:
  ..\gengetopt-2.13.1\Release\gengetopt.exe -u -N -e -i SVMer.ggo 

  The developers of gengetopt consider the fixed text that goes in all
  gengetopt output files to be in the public domain:
  we make no copyright claims on it.
*/

/* If we use autoconf.  */
#ifdef HAVE_CONFIG_H
#include "config.h"
#endif

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#include "getopt.h"

#include "cmdline.h"

static
void clear_given (struct gengetopt_args_info *args_info);
static
void clear_args (struct gengetopt_args_info *args_info);

static int
cmdline_parser_internal (int argc, char * const *argv, struct gengetopt_args_info *args_info, int override, int initialize, int check_required, const char *additional_error);


char *kernel_values[] = {"linear", "poly", "rbf", 0} ;	/* Possible values for kernel.  */

static char *
gengetopt_strdup (const char *s);

static
void clear_given (struct gengetopt_args_info *args_info)
{
  args_info->help_given = 0 ;
  args_info->version_given = 0 ;
  args_info->input_given = 0 ;
  args_info->output_given = 0 ;
  args_info->model_given = 0 ;
  args_info->pcl_given = 0 ;
  args_info->genewise_given = 0 ;
  args_info->skip_given = 0 ;
  args_info->binary_given = 0 ;
  args_info->genes_given = 0 ;
  args_info->genex_given = 0 ;
  args_info->genet_given = 0 ;
  args_info->genel_given = 0 ;
  args_info->random_given = 0 ;
  args_info->verbosity_given = 0 ;
  args_info->kernel_given = 0 ;
  args_info->cache_given = 0 ;
  args_info->tradeoff_given = 0 ;
  args_info->gamma_given = 0 ;
  args_info->degree_given = 0 ;
  args_info->alphas_given = 0 ;
  args_info->iterations_given = 0 ;
}

static
void clear_args (struct gengetopt_args_info *args_info)
{
  args_info->input_arg = NULL;
  args_info->output_arg = NULL;
  args_info->model_arg = NULL;
  args_info->pcl_flag = 1;
  args_info->genewise_flag = 0;
  args_info->skip_arg = 2 ;
  args_info->binary_arg = NULL;
  args_info->genes_arg = NULL;
  args_info->genex_arg = NULL;
  args_info->genet_arg = NULL;
  args_info->genel_arg = NULL;
  args_info->random_arg = 0 ;
  args_info->verbosity_arg = 5 ;
  args_info->kernel_arg = gengetopt_strdup ("linear");
  args_info->cache_arg = 40 ;
  args_info->gamma_arg = 1 ;
  args_info->degree_arg = 3 ;
  args_info->alphas_arg = NULL;
  args_info->iterations_arg = 100000 ;
}

void
cmdline_parser_print_version (void)
{
  printf ("%s %s\n", CMDLINE_PARSER_PACKAGE, CMDLINE_PARSER_VERSION);
}

void
cmdline_parser_print_help (void)
{
  cmdline_parser_print_version ();
  printf("\n%s\n", "SVM training and evaluation");
  printf("\nUsage: SVMer [OPTIONS]... [FILES]...\n\n");
  printf("%s\n","  -h, --help             Print help and exit");
  printf("%s\n","  -V, --version          Print version and exit");
  printf("%s\n","\nOptional:");
  printf("%s\n","  -i, --input=filename   Input answer DAT file");
  printf("%s\n","  -o, --output=filename  Output prediction DAT file");
  printf("%s\n","  -m, --model=filename   SVM model file");
  printf("%s\n","  -p, --pcl              PCL input mode  (default=on)");
  printf("%s\n","  -w, --genewise         Learn per-gene SVMs for pairwise predictions  \n                           (default=off)");
  printf("%s\n","  -s, --skip=INT         Columns to skip in input PCLs  (default=`2')");
  printf("%s\n","  -b, --binary=filename  Input binary training file");
  printf("%s\n","  -g, --genes=filename   Gene inclusion file");
  printf("%s\n","  -G, --genex=filename   Gene exclusion file");
  printf("%s\n","  -c, --genet=filename   Term inclusion file");
  printf("%s\n","  -l, --genel=filename   Gene skip file for per-gene SVMs");
  printf("%s\n","  -r, --random=INT       Seed random generator  (default=`0')");
  printf("%s\n","  -v, --verbosity=INT    Message verbosity  (default=`5')");
  printf("%s\n","\nSVM:");
  printf("%s\n","  -k, --kernel=STRING    SVM kernel function  (possible values=\"linear\", \n                           \"poly\", \"rbf\" default=`linear')");
  printf("%s\n","  -e, --cache=INT        SVM cache size  (default=`40')");
  printf("%s\n","  -C, --tradeoff=FLOAT   Classification tradeoff");
  printf("%s\n","  -M, --gamma=FLOAT      RBF gamma  (default=`1')");
  printf("%s\n","  -d, --degree=INT       Polynomial degree  (default=`3')");
  printf("%s\n","  -a, --alphas=filename  SVM alphas file");
  printf("%s\n","  -t, --iterations=INT   SVM iterations  (default=`100000')");
  
}

void
cmdline_parser_init (struct gengetopt_args_info *args_info)
{
  clear_given (args_info);
  clear_args (args_info);

  args_info->inputs = NULL;
  args_info->inputs_num = 0;
}

void
cmdline_parser_free (struct gengetopt_args_info *args_info)
{
  
  unsigned int i;
  if (args_info->input_arg)
    {
      free (args_info->input_arg); /* free previous argument */
      args_info->input_arg = 0;
    }
  if (args_info->output_arg)
    {
      free (args_info->output_arg); /* free previous argument */
      args_info->output_arg = 0;
    }
  if (args_info->model_arg)
    {
      free (args_info->model_arg); /* free previous argument */
      args_info->model_arg = 0;
    }
  if (args_info->binary_arg)
    {
      free (args_info->binary_arg); /* free previous argument */
      args_info->binary_arg = 0;
    }
  if (args_info->genes_arg)
    {
      free (args_info->genes_arg); /* free previous argument */
      args_info->genes_arg = 0;
    }
  if (args_info->genex_arg)
    {
      free (args_info->genex_arg); /* free previous argument */
      args_info->genex_arg = 0;
    }
  if (args_info->genet_arg)
    {
      free (args_info->genet_arg); /* free previous argument */
      args_info->genet_arg = 0;
    }
  if (args_info->genel_arg)
    {
      free (args_info->genel_arg); /* free previous argument */
      args_info->genel_arg = 0;
    }
  if (args_info->kernel_arg)
    {
      free (args_info->kernel_arg); /* free previous argument */
      args_info->kernel_arg = 0;
    }
  if (args_info->alphas_arg)
    {
      free (args_info->alphas_arg); /* free previous argument */
      args_info->alphas_arg = 0;
    }
  
  for (i = 0; i < args_info->inputs_num; ++i)
    free (args_info->inputs [i]);
  
  if (args_info->inputs_num)
    free (args_info->inputs);
  
  clear_given (args_info);
}

static int
check_possible_values(const char *val, char *values[])
{
  int i, found;
  size_t len;

  if (!val)   /* otherwise strlen() crashes below */
    return 0; /* NULL means no argument for the option */

  for (found = i = 0, len = strlen(val); values[i]; ++i)
    {
      if (strncmp(val, values[i], len) == 0)
        {
          found++;
          if (strlen(values[i]) == len)
            return 1; /* exact macth no need to check more */
        }
    }

  return found; /* return how many values are matched */
}

/* gengetopt_strdup() */
/* strdup.c replacement of strdup, which is not standard */
char *
gengetopt_strdup (const char *s)
{
  char *result = NULL;
  if (!s)
    return result;

  result = (char*)malloc(strlen(s) + 1);
  if (result == (char*)0)
    return (char*)0;
  strcpy(result, s);
  return result;
}

int
cmdline_parser (int argc, char * const *argv, struct gengetopt_args_info *args_info)
{
  return cmdline_parser2 (argc, argv, args_info, 0, 1, 1);
}

int
cmdline_parser2 (int argc, char * const *argv, struct gengetopt_args_info *args_info, int override, int initialize, int check_required)
{
  int result;

  result = cmdline_parser_internal (argc, argv, args_info, override, initialize, check_required, NULL);

  return result;
}

int
cmdline_parser_required (struct gengetopt_args_info *args_info, const char *prog_name)
{
  return EXIT_SUCCESS;
}

int
cmdline_parser_internal (int argc, char * const *argv, struct gengetopt_args_info *args_info, int override, int initialize, int check_required, const char *additional_error)
{
  int c;	/* Character of the parsed option.  */

  int error = 0;
  struct gengetopt_args_info local_args_info;

  if (initialize)
    cmdline_parser_init (args_info);

  cmdline_parser_init (&local_args_info);

  optarg = 0;
  optind = 1;
  opterr = 1;
  optopt = '?';

  while (1)
    {
      int found = 0;
      int option_index = 0;
      char *stop_char;

      static struct option long_options[] = {
        { "help",	0, NULL, 'h' },
        { "version",	0, NULL, 'V' },
        { "input",	1, NULL, 'i' },
        { "output",	1, NULL, 'o' },
        { "model",	1, NULL, 'm' },
        { "pcl",	0, NULL, 'p' },
        { "genewise",	0, NULL, 'w' },
        { "skip",	1, NULL, 's' },
        { "binary",	1, NULL, 'b' },
        { "genes",	1, NULL, 'g' },
        { "genex",	1, NULL, 'G' },
        { "genet",	1, NULL, 'c' },
        { "genel",	1, NULL, 'l' },
        { "random",	1, NULL, 'r' },
        { "verbosity",	1, NULL, 'v' },
        { "kernel",	1, NULL, 'k' },
        { "cache",	1, NULL, 'e' },
        { "tradeoff",	1, NULL, 'C' },
        { "gamma",	1, NULL, 'M' },
        { "degree",	1, NULL, 'd' },
        { "alphas",	1, NULL, 'a' },
        { "iterations",	1, NULL, 't' },
        { NULL,	0, NULL, 0 }
      };

      stop_char = 0;
      c = getopt_long (argc, argv, "hVi:o:m:pws:b:g:G:c:l:r:v:k:e:C:M:d:a:t:", long_options, &option_index);

      if (c == -1) break;	/* Exit from `while (1)' loop.  */

      switch (c)
        {
        case 'h':	/* Print help and exit.  */
          cmdline_parser_print_help ();
          cmdline_parser_free (&local_args_info);
          exit (EXIT_SUCCESS);

        case 'V':	/* Print version and exit.  */
          if (local_args_info.version_given)
            {
              fprintf (stderr, "%s: `--version' (`-V') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->version_given && ! override)
            continue;
          local_args_info.version_given = 1;
          args_info->version_given = 1;
          cmdline_parser_free (&local_args_info);
          return 0;

        case 'i':	/* Input answer DAT file.  */
          if (local_args_info.input_given)
            {
              fprintf (stderr, "%s: `--input' (`-i') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->input_given && ! override)
            continue;
          local_args_info.input_given = 1;
          args_info->input_given = 1;
          if (args_info->input_arg)
            free (args_info->input_arg); /* free previous string */
          args_info->input_arg = gengetopt_strdup (optarg);
          break;

        case 'o':	/* Output prediction DAT file.  */
          if (local_args_info.output_given)
            {
              fprintf (stderr, "%s: `--output' (`-o') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->output_given && ! override)
            continue;
          local_args_info.output_given = 1;
          args_info->output_given = 1;
          if (args_info->output_arg)
            free (args_info->output_arg); /* free previous string */
          args_info->output_arg = gengetopt_strdup (optarg);
          break;

        case 'm':	/* SVM model file.  */
          if (local_args_info.model_given)
            {
              fprintf (stderr, "%s: `--model' (`-m') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->model_given && ! override)
            continue;
          local_args_info.model_given = 1;
          args_info->model_given = 1;
          if (args_info->model_arg)
            free (args_info->model_arg); /* free previous string */
          args_info->model_arg = gengetopt_strdup (optarg);
          break;

        case 'p':	/* PCL input mode.  */
          if (local_args_info.pcl_given)
            {
              fprintf (stderr, "%s: `--pcl' (`-p') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->pcl_given && ! override)
            continue;
          local_args_info.pcl_given = 1;
          args_info->pcl_given = 1;
          args_info->pcl_flag = !(args_info->pcl_flag);
          break;

        case 'w':	/* Learn per-gene SVMs for pairwise predictions.  */
          if (local_args_info.genewise_given)
            {
              fprintf (stderr, "%s: `--genewise' (`-w') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->genewise_given && ! override)
            continue;
          local_args_info.genewise_given = 1;
          args_info->genewise_given = 1;
          args_info->genewise_flag = !(args_info->genewise_flag);
          break;

        case 's':	/* Columns to skip in input PCLs.  */
          if (local_args_info.skip_given)
            {
              fprintf (stderr, "%s: `--skip' (`-s') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->skip_given && ! override)
            continue;
          local_args_info.skip_given = 1;
          args_info->skip_given = 1;
          args_info->skip_arg = strtol (optarg,&stop_char,0);
          break;

        case 'b':	/* Input binary training file.  */
          if (local_args_info.binary_given)
            {
              fprintf (stderr, "%s: `--binary' (`-b') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->binary_given && ! override)
            continue;
          local_args_info.binary_given = 1;
          args_info->binary_given = 1;
          if (args_info->binary_arg)
            free (args_info->binary_arg); /* free previous string */
          args_info->binary_arg = gengetopt_strdup (optarg);
          break;

        case 'g':	/* Gene inclusion file.  */
          if (local_args_info.genes_given)
            {
              fprintf (stderr, "%s: `--genes' (`-g') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->genes_given && ! override)
            continue;
          local_args_info.genes_given = 1;
          args_info->genes_given = 1;
          if (args_info->genes_arg)
            free (args_info->genes_arg); /* free previous string */
          args_info->genes_arg = gengetopt_strdup (optarg);
          break;

        case 'G':	/* Gene exclusion file.  */
          if (local_args_info.genex_given)
            {
              fprintf (stderr, "%s: `--genex' (`-G') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->genex_given && ! override)
            continue;
          local_args_info.genex_given = 1;
          args_info->genex_given = 1;
          if (args_info->genex_arg)
            free (args_info->genex_arg); /* free previous string */
          args_info->genex_arg = gengetopt_strdup (optarg);
          break;

        case 'c':	/* Term inclusion file.  */
          if (local_args_info.genet_given)
            {
              fprintf (stderr, "%s: `--genet' (`-c') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->genet_given && ! override)
            continue;
          local_args_info.genet_given = 1;
          args_info->genet_given = 1;
          if (args_info->genet_arg)
            free (args_info->genet_arg); /* free previous string */
          args_info->genet_arg = gengetopt_strdup (optarg);
          break;

        case 'l':	/* Gene skip file for per-gene SVMs.  */
          if (local_args_info.genel_given)
            {
              fprintf (stderr, "%s: `--genel' (`-l') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->genel_given && ! override)
            continue;
          local_args_info.genel_given = 1;
          args_info->genel_given = 1;
          if (args_info->genel_arg)
            free (args_info->genel_arg); /* free previous string */
          args_info->genel_arg = gengetopt_strdup (optarg);
          break;

        case 'r':	/* Seed random generator.  */
          if (local_args_info.random_given)
            {
              fprintf (stderr, "%s: `--random' (`-r') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->random_given && ! override)
            continue;
          local_args_info.random_given = 1;
          args_info->random_given = 1;
          args_info->random_arg = strtol (optarg,&stop_char,0);
          break;

        case 'v':	/* Message verbosity.  */
          if (local_args_info.verbosity_given)
            {
              fprintf (stderr, "%s: `--verbosity' (`-v') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->verbosity_given && ! override)
            continue;
          local_args_info.verbosity_given = 1;
          args_info->verbosity_given = 1;
          args_info->verbosity_arg = strtol (optarg,&stop_char,0);
          break;

        case 'k':	/* SVM kernel function.  */
          if (local_args_info.kernel_given)
            {
              fprintf (stderr, "%s: `--kernel' (`-k') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if ((found = check_possible_values(optarg, kernel_values)) != 1)
            {
              fprintf (stderr, "%s: %s argument, \"%s\", for option `--kernel' (`-k')%s\n", argv[0], found ? "ambiguous" : "invalid", optarg, (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->kernel_given && ! override)
            continue;
          local_args_info.kernel_given = 1;
          args_info->kernel_given = 1;
          if (args_info->kernel_arg)
            free (args_info->kernel_arg); /* free previous string */
          args_info->kernel_arg = gengetopt_strdup (optarg);
          break;

        case 'e':	/* SVM cache size.  */
          if (local_args_info.cache_given)
            {
              fprintf (stderr, "%s: `--cache' (`-e') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->cache_given && ! override)
            continue;
          local_args_info.cache_given = 1;
          args_info->cache_given = 1;
          args_info->cache_arg = strtol (optarg,&stop_char,0);
          break;

        case 'C':	/* Classification tradeoff.  */
          if (local_args_info.tradeoff_given)
            {
              fprintf (stderr, "%s: `--tradeoff' (`-C') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->tradeoff_given && ! override)
            continue;
          local_args_info.tradeoff_given = 1;
          args_info->tradeoff_given = 1;
          args_info->tradeoff_arg = (float)strtod (optarg, NULL);
          break;

        case 'M':	/* RBF gamma.  */
          if (local_args_info.gamma_given)
            {
              fprintf (stderr, "%s: `--gamma' (`-M') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->gamma_given && ! override)
            continue;
          local_args_info.gamma_given = 1;
          args_info->gamma_given = 1;
          args_info->gamma_arg = (float)strtod (optarg, NULL);
          break;

        case 'd':	/* Polynomial degree.  */
          if (local_args_info.degree_given)
            {
              fprintf (stderr, "%s: `--degree' (`-d') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->degree_given && ! override)
            continue;
          local_args_info.degree_given = 1;
          args_info->degree_given = 1;
          args_info->degree_arg = strtol (optarg,&stop_char,0);
          break;

        case 'a':	/* SVM alphas file.  */
          if (local_args_info.alphas_given)
            {
              fprintf (stderr, "%s: `--alphas' (`-a') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->alphas_given && ! override)
            continue;
          local_args_info.alphas_given = 1;
          args_info->alphas_given = 1;
          if (args_info->alphas_arg)
            free (args_info->alphas_arg); /* free previous string */
          args_info->alphas_arg = gengetopt_strdup (optarg);
          break;

        case 't':	/* SVM iterations.  */
          if (local_args_info.iterations_given)
            {
              fprintf (stderr, "%s: `--iterations' (`-t') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->iterations_given && ! override)
            continue;
          local_args_info.iterations_given = 1;
          args_info->iterations_given = 1;
          args_info->iterations_arg = strtol (optarg,&stop_char,0);
          break;


        case 0:	/* Long option with no short option */
        case '?':	/* Invalid option.  */
          /* `getopt_long' already printed an error message.  */
          goto failure;

        default:	/* bug: option not considered.  */
          fprintf (stderr, "%s: option unknown: %c%s\n", CMDLINE_PARSER_PACKAGE, c, (additional_error ? additional_error : ""));
          abort ();
        } /* switch */
    } /* while */




  cmdline_parser_free (&local_args_info);

  if ( error )
    return (EXIT_FAILURE);

  if (optind < argc)
    {
      int i = 0 ;

      args_info->inputs_num = argc - optind ;
      args_info->inputs =
        (char **)(malloc ((args_info->inputs_num)*sizeof(char *))) ;
      while (optind < argc)
        args_info->inputs[ i++ ] = gengetopt_strdup (argv[optind++]) ;
    }

  return 0;

failure:
  
  cmdline_parser_free (&local_args_info);
  return (EXIT_FAILURE);
}

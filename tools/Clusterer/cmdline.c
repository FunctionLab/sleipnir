/*
  File autogenerated by gengetopt version 2.13.1
  generated with the following command:
  ../gengetopt-2.13.1/bin/gengetopt -N -e -i Clusterer.ggo 

  The developers of gengetopt consider the fixed text that goes in all
  gengetopt output files to be in the public domain:
  we make no copyright claims on it.
*/

/* If we use autoconf.  */
#ifdef HAVE_CONFIG_H
#include "config.h"
#endif

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#include "getopt.h"

#include "cmdline.h"

static
void clear_given (struct gengetopt_args_info *args_info);
static
void clear_args (struct gengetopt_args_info *args_info);

static int
cmdline_parser_internal (int argc, char * const *argv, struct gengetopt_args_info *args_info, int override, int initialize, int check_required, const char *additional_error);


char *distance_values[] = {"pearson", "euclidean", "kendalls", "kolm-smir", "spearman", "quickpear", 0} ;	/* Possible values for distance.  */

static char *
gengetopt_strdup (const char *s);

static
void clear_given (struct gengetopt_args_info *args_info)
{
  args_info->help_given = 0 ;
  args_info->version_given = 0 ;
  args_info->input_given = 0 ;
  args_info->output_given = 0 ;
  args_info->distance_given = 0 ;
  args_info->skip_given = 0 ;
  args_info->diameter_given = 0 ;
  args_info->diamineter_given = 0 ;
  args_info->delta_given = 0 ;
  args_info->size_given = 0 ;
  args_info->autocorrelate_given = 0 ;
  args_info->normalize_given = 0 ;
  args_info->verbosity_given = 0 ;
  args_info->random_given = 0 ;
  args_info->weights_given = 0 ;
}

static
void clear_args (struct gengetopt_args_info *args_info)
{
  args_info->input_arg = NULL;
  args_info->output_arg = NULL;
  args_info->distance_arg = gengetopt_strdup ("quickpear");
  args_info->skip_arg = 2 ;
  args_info->diameter_arg = 0.5 ;
  args_info->diamineter_arg = 0 ;
  args_info->delta_arg = 0 ;
  args_info->size_arg = 10 ;
  args_info->autocorrelate_flag = 0;
  args_info->normalize_flag = 1;
  args_info->verbosity_arg = 5 ;
  args_info->random_arg = 0 ;
  args_info->weights_arg = NULL;
}

void
cmdline_parser_print_version (void)
{
  printf ("%s %s\n", CMDLINE_PARSER_PACKAGE, CMDLINE_PARSER_VERSION);
}

void
cmdline_parser_print_help (void)
{
  cmdline_parser_print_version ();
  printf("\n%s\n", "QTC and other hard clustering methods");
  printf("\nUsage: Clusterer [OPTIONS]...\n\n");
  printf("%s\n","  -h, --help               Print help and exit");
  printf("%s\n","  -V, --version            Print version and exit");
  printf("%s\n","\nOptional:");
  printf("%s\n","  -i, --input=filename     Input PCL file");
  printf("%s\n","  -o, --output=filename    Output DAB file");
  printf("%s\n","  -d, --distance=STRING    Distance measure  (possible values=\"pearson\", \n                             \"euclidean\", \"kendalls\", \"kolm-smir\", \n                             \"spearman\", \"quickpear\" default=`quickpear')");
  printf("%s\n","  -s, --skip=INT           Columns to skip in input PCL  (default=`2')");
  printf("%s\n","  -m, --diameter=DOUBLE    Maximum cluster diameter  (default=`0.5')");
  printf("%s\n","  -M, --diamineter=DOUBLE  Minimum cluster diameter  (default=`0')");
  printf("%s\n","  -e, --delta=DOUBLE       Cluster diameter step size  (default=`0')");
  printf("%s\n","  -z, --size=INT           Minimum cluster size  (default=`10')");
  printf("%s\n","  -a, --autocorrelate      Autocorrelate similarity measures  (default=off)");
  printf("%s\n","  -n, --normalize          Normalize distances before clustering  (default=on)");
  printf("%s\n","  -v, --verbosity=INT      Message verbosity  (default=`5')");
  printf("%s\n","  -r, --random=INT         Seed random generator  (default=`0')");
  printf("%s\n","  -w, --weights=filename   Input weights file");
  
}

void
cmdline_parser_init (struct gengetopt_args_info *args_info)
{
  clear_given (args_info);
  clear_args (args_info);
}

void
cmdline_parser_free (struct gengetopt_args_info *args_info)
{
  
  if (args_info->input_arg)
    {
      free (args_info->input_arg); /* free previous argument */
      args_info->input_arg = 0;
    }
  if (args_info->output_arg)
    {
      free (args_info->output_arg); /* free previous argument */
      args_info->output_arg = 0;
    }
  if (args_info->distance_arg)
    {
      free (args_info->distance_arg); /* free previous argument */
      args_info->distance_arg = 0;
    }
  if (args_info->weights_arg)
    {
      free (args_info->weights_arg); /* free previous argument */
      args_info->weights_arg = 0;
    }
  
  clear_given (args_info);
}

static int
check_possible_values(const char *val, char *values[])
{
  int i, found;
  size_t len;

  if (!val)   /* otherwise strlen() crashes below */
    return 0; /* NULL means no argument for the option */

  for (found = i = 0, len = strlen(val); values[i]; ++i)
    {
      if (strncmp(val, values[i], len) == 0)
        {
          found++;
          if (strlen(values[i]) == len)
            return 1; /* exact macth no need to check more */
        }
    }

  return found; /* return how many values are matched */
}

/* gengetopt_strdup() */
/* strdup.c replacement of strdup, which is not standard */
char *
gengetopt_strdup (const char *s)
{
  char *result = NULL;
  if (!s)
    return result;

  result = (char*)malloc(strlen(s) + 1);
  if (result == (char*)0)
    return (char*)0;
  strcpy(result, s);
  return result;
}

int
cmdline_parser (int argc, char * const *argv, struct gengetopt_args_info *args_info)
{
  return cmdline_parser2 (argc, argv, args_info, 0, 1, 1);
}

int
cmdline_parser2 (int argc, char * const *argv, struct gengetopt_args_info *args_info, int override, int initialize, int check_required)
{
  int result;

  result = cmdline_parser_internal (argc, argv, args_info, override, initialize, check_required, NULL);

  return result;
}

int
cmdline_parser_required (struct gengetopt_args_info *args_info, const char *prog_name)
{
  return EXIT_SUCCESS;
}

int
cmdline_parser_internal (int argc, char * const *argv, struct gengetopt_args_info *args_info, int override, int initialize, int check_required, const char *additional_error)
{
  int c;	/* Character of the parsed option.  */

  int error = 0;
  struct gengetopt_args_info local_args_info;

  if (initialize)
    cmdline_parser_init (args_info);

  cmdline_parser_init (&local_args_info);

  optarg = 0;
  optind = 1;
  opterr = 1;
  optopt = '?';

  while (1)
    {
      int found = 0;
      int option_index = 0;
      char *stop_char;

      static struct option long_options[] = {
        { "help",	0, NULL, 'h' },
        { "version",	0, NULL, 'V' },
        { "input",	1, NULL, 'i' },
        { "output",	1, NULL, 'o' },
        { "distance",	1, NULL, 'd' },
        { "skip",	1, NULL, 's' },
        { "diameter",	1, NULL, 'm' },
        { "diamineter",	1, NULL, 'M' },
        { "delta",	1, NULL, 'e' },
        { "size",	1, NULL, 'z' },
        { "autocorrelate",	0, NULL, 'a' },
        { "normalize",	0, NULL, 'n' },
        { "verbosity",	1, NULL, 'v' },
        { "random",	1, NULL, 'r' },
        { "weights",	1, NULL, 'w' },
        { NULL,	0, NULL, 0 }
      };

      stop_char = 0;
      c = getopt_long (argc, argv, "hVi:o:d:s:m:M:e:z:anv:r:w:", long_options, &option_index);

      if (c == -1) break;	/* Exit from `while (1)' loop.  */

      switch (c)
        {
        case 'h':	/* Print help and exit.  */
          cmdline_parser_print_help ();
          cmdline_parser_free (&local_args_info);
          exit (EXIT_SUCCESS);

        case 'V':	/* Print version and exit.  */
          if (local_args_info.version_given)
            {
              fprintf (stderr, "%s: `--version' (`-V') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->version_given && ! override)
            continue;
          local_args_info.version_given = 1;
          args_info->version_given = 1;
          cmdline_parser_free (&local_args_info);
          return 0;

        case 'i':	/* Input PCL file.  */
          if (local_args_info.input_given)
            {
              fprintf (stderr, "%s: `--input' (`-i') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->input_given && ! override)
            continue;
          local_args_info.input_given = 1;
          args_info->input_given = 1;
          if (args_info->input_arg)
            free (args_info->input_arg); /* free previous string */
          args_info->input_arg = gengetopt_strdup (optarg);
          break;

        case 'o':	/* Output DAB file.  */
          if (local_args_info.output_given)
            {
              fprintf (stderr, "%s: `--output' (`-o') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->output_given && ! override)
            continue;
          local_args_info.output_given = 1;
          args_info->output_given = 1;
          if (args_info->output_arg)
            free (args_info->output_arg); /* free previous string */
          args_info->output_arg = gengetopt_strdup (optarg);
          break;

        case 'd':	/* Distance measure.  */
          if (local_args_info.distance_given)
            {
              fprintf (stderr, "%s: `--distance' (`-d') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if ((found = check_possible_values(optarg, distance_values)) != 1)
            {
              fprintf (stderr, "%s: %s argument, \"%s\", for option `--distance' (`-d')%s\n", argv[0], found ? "ambiguous" : "invalid", optarg, (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->distance_given && ! override)
            continue;
          local_args_info.distance_given = 1;
          args_info->distance_given = 1;
          if (args_info->distance_arg)
            free (args_info->distance_arg); /* free previous string */
          args_info->distance_arg = gengetopt_strdup (optarg);
          break;

        case 's':	/* Columns to skip in input PCL.  */
          if (local_args_info.skip_given)
            {
              fprintf (stderr, "%s: `--skip' (`-s') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->skip_given && ! override)
            continue;
          local_args_info.skip_given = 1;
          args_info->skip_given = 1;
          args_info->skip_arg = strtol (optarg,&stop_char,0);
          break;

        case 'm':	/* Maximum cluster diameter.  */
          if (local_args_info.diameter_given)
            {
              fprintf (stderr, "%s: `--diameter' (`-m') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->diameter_given && ! override)
            continue;
          local_args_info.diameter_given = 1;
          args_info->diameter_given = 1;
          args_info->diameter_arg = strtod (optarg, NULL);
          break;

        case 'M':	/* Minimum cluster diameter.  */
          if (local_args_info.diamineter_given)
            {
              fprintf (stderr, "%s: `--diamineter' (`-M') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->diamineter_given && ! override)
            continue;
          local_args_info.diamineter_given = 1;
          args_info->diamineter_given = 1;
          args_info->diamineter_arg = strtod (optarg, NULL);
          break;

        case 'e':	/* Cluster diameter step size.  */
          if (local_args_info.delta_given)
            {
              fprintf (stderr, "%s: `--delta' (`-e') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->delta_given && ! override)
            continue;
          local_args_info.delta_given = 1;
          args_info->delta_given = 1;
          args_info->delta_arg = strtod (optarg, NULL);
          break;

        case 'z':	/* Minimum cluster size.  */
          if (local_args_info.size_given)
            {
              fprintf (stderr, "%s: `--size' (`-z') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->size_given && ! override)
            continue;
          local_args_info.size_given = 1;
          args_info->size_given = 1;
          args_info->size_arg = strtol (optarg,&stop_char,0);
          break;

        case 'a':	/* Autocorrelate similarity measures.  */
          if (local_args_info.autocorrelate_given)
            {
              fprintf (stderr, "%s: `--autocorrelate' (`-a') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->autocorrelate_given && ! override)
            continue;
          local_args_info.autocorrelate_given = 1;
          args_info->autocorrelate_given = 1;
          args_info->autocorrelate_flag = !(args_info->autocorrelate_flag);
          break;

        case 'n':	/* Normalize distances before clustering.  */
          if (local_args_info.normalize_given)
            {
              fprintf (stderr, "%s: `--normalize' (`-n') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->normalize_given && ! override)
            continue;
          local_args_info.normalize_given = 1;
          args_info->normalize_given = 1;
          args_info->normalize_flag = !(args_info->normalize_flag);
          break;

        case 'v':	/* Message verbosity.  */
          if (local_args_info.verbosity_given)
            {
              fprintf (stderr, "%s: `--verbosity' (`-v') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->verbosity_given && ! override)
            continue;
          local_args_info.verbosity_given = 1;
          args_info->verbosity_given = 1;
          args_info->verbosity_arg = strtol (optarg,&stop_char,0);
          break;

        case 'r':	/* Seed random generator.  */
          if (local_args_info.random_given)
            {
              fprintf (stderr, "%s: `--random' (`-r') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->random_given && ! override)
            continue;
          local_args_info.random_given = 1;
          args_info->random_given = 1;
          args_info->random_arg = strtol (optarg,&stop_char,0);
          break;

        case 'w':	/* Input weights file.  */
          if (local_args_info.weights_given)
            {
              fprintf (stderr, "%s: `--weights' (`-w') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->weights_given && ! override)
            continue;
          local_args_info.weights_given = 1;
          args_info->weights_given = 1;
          if (args_info->weights_arg)
            free (args_info->weights_arg); /* free previous string */
          args_info->weights_arg = gengetopt_strdup (optarg);
          break;


        case 0:	/* Long option with no short option */
        case '?':	/* Invalid option.  */
          /* `getopt_long' already printed an error message.  */
          goto failure;

        default:	/* bug: option not considered.  */
          fprintf (stderr, "%s: option unknown: %c%s\n", CMDLINE_PARSER_PACKAGE, c, (additional_error ? additional_error : ""));
          abort ();
        } /* switch */
    } /* while */




  cmdline_parser_free (&local_args_info);

  if ( error )
    return (EXIT_FAILURE);

  return 0;

failure:
  
  cmdline_parser_free (&local_args_info);
  return (EXIT_FAILURE);
}

/*
  File autogenerated by gengetopt version 2.13.1
  generated with the following command:
  ..\gengetopt-2.13.1\Release\gengetopt.exe -C -N -e -i BNFunc.ggo 

  The developers of gengetopt consider the fixed text that goes in all
  gengetopt output files to be in the public domain:
  we make no copyright claims on it.
*/

/* If we use autoconf.  */
#ifdef HAVE_CONFIG_H
#include "config.h"
#endif

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#include "getopt.h"

#include "cmdline.h"

static
void clear_given (struct gengetopt_args_info *args_info);
static
void clear_args (struct gengetopt_args_info *args_info);

static int
cmdline_parser_internal (int argc, char * const *argv, struct gengetopt_args_info *args_info, int override, int initialize, int check_required, const char *additional_error);

static int
cmdline_parser_required2 (struct gengetopt_args_info *args_info, const char *prog_name, const char *additional_error);

char *go_name_values[] = {"cc", "bp", "mf", 0} ;	/* Possible values for go_name.  */

static char *
gengetopt_strdup (const char *s);

static
void clear_given (struct gengetopt_args_info *args_info)
{
  args_info->help_given = 0 ;
  args_info->version_given = 0 ;
  args_info->input_given = 0 ;
  args_info->output_given = 0 ;
  args_info->negatives_given = 0 ;
  args_info->directory_given = 0 ;
  args_info->kegg_given = 0 ;
  args_info->kegg_org_given = 0 ;
  args_info->go_onto_given = 0 ;
  args_info->go_anno_given = 0 ;
  args_info->go_name_given = 0 ;
  args_info->mips_onto_given = 0 ;
  args_info->mips_anno_given = 0 ;
  args_info->random_given = 0 ;
  args_info->test_given = 0 ;
  args_info->synonyms_given = 0 ;
  args_info->sql_given = 0 ;
  args_info->verbosity_given = 0 ;
  args_info->config_given = 0 ;
  args_info->dbids_given = 0 ;
  args_info->allids_given = 0 ;
  args_info->nsets_given = 0 ;
  args_info->nsetlap_given = 0 ;
}

static
void clear_args (struct gengetopt_args_info *args_info)
{
  args_info->input_arg = NULL;
  args_info->output_arg = NULL;
  args_info->negatives_arg = NULL;
  args_info->directory_arg = gengetopt_strdup (".");
  args_info->kegg_arg = NULL;
  args_info->kegg_org_arg = gengetopt_strdup ("SCE");
  args_info->go_onto_arg = NULL;
  args_info->go_anno_arg = NULL;
  args_info->go_name_arg = gengetopt_strdup ("bp");
  args_info->mips_onto_arg = NULL;
  args_info->mips_anno_arg = NULL;
  args_info->random_arg = 0 ;
  args_info->test_arg = 0 ;
  args_info->synonyms_flag = 0;
  args_info->sql_arg = NULL;
  args_info->verbosity_arg = 5 ;
  args_info->config_arg = gengetopt_strdup ("BNFunc.ini");
  args_info->dbids_flag = 0;
  args_info->allids_flag = 0;
  args_info->nsets_flag = 0;
  args_info->nsetlap_arg = 0.05 ;
}

void
cmdline_parser_print_version (void)
{
  printf ("%s %s\n", CMDLINE_PARSER_PACKAGE, CMDLINE_PARSER_VERSION);
}

void
cmdline_parser_print_help (void)
{
  cmdline_parser_print_version ();
  printf("\n%s\n", "Functional Bayes net preparation");
  printf("\nUsage: BNFunc [OPTIONS]...\n\n");
  printf("%s\n","  -h, --help                 Print help and exit");
  printf("%s\n","  -V, --version              Print version and exit");
  printf("%s\n","  -i, --input=filename       Ontology slim file");
  printf("%s\n","\nOptional:");
  printf("%s\n","  -o, --output=filename      Answer file");
  printf("%s\n","  -I, --negatives=filename   Negative slim file");
  printf("%s\n","  -d, --directory=directory  Output directory  (default=`.')");
  printf("%s\n","  -k, --kegg=filename        KEGG ontology");
  printf("%s\n","  -K, --kegg_org=STRING      KEGG organism  (default=`SCE')");
  printf("%s\n","  -y, --go_onto=filename     GO ontology");
  printf("%s\n","  -g, --go_anno=filename     GO annotations");
  printf("%s\n","  -n, --go_name=STRING       GO namespace  (possible values=\"cc\", \"bp\", \n                               \"mf\" default=`bp')");
  printf("%s\n","  -m, --mips_onto=filename   MIPS ontology");
  printf("%s\n","  -a, --mips_anno=filename   MIPS annotations");
  printf("%s\n","  -r, --random=INT           Seed random generator  (default=`0')");
  printf("%s\n","  -t, --test=DOUBLE          Test fraction  (default=`0')");
  printf("%s\n","  -s, --synonyms             Prefer synonym names  (default=off)");
  printf("%s\n","  -q, --sql=filename         Directory in which to place SQL tables");
  printf("%s\n","  -v, --verbosity=INT        Message verbosity  (default=`5')");
  printf("%s\n","  -c, --config=filename      Command line config file  (default=`BNFunc.ini')");
  printf("%s\n","  -b, --dbids                Include GO database IDs  (default=off)");
  printf("%s\n","  -l, --allids               Output all available IDs  (default=off)");
  printf("%s\n","  -N, --nsets                Generate negative sets for input slim  \n                               (default=off)");
  printf("%s\n","  -L, --nsetlap=DOUBLE       P-value of overlap for negative rejection  \n                               (default=`0.05')");
  
}

void
cmdline_parser_init (struct gengetopt_args_info *args_info)
{
  clear_given (args_info);
  clear_args (args_info);
}

void
cmdline_parser_free (struct gengetopt_args_info *args_info)
{
  
  if (args_info->input_arg)
    {
      free (args_info->input_arg); /* free previous argument */
      args_info->input_arg = 0;
    }
  if (args_info->output_arg)
    {
      free (args_info->output_arg); /* free previous argument */
      args_info->output_arg = 0;
    }
  if (args_info->negatives_arg)
    {
      free (args_info->negatives_arg); /* free previous argument */
      args_info->negatives_arg = 0;
    }
  if (args_info->directory_arg)
    {
      free (args_info->directory_arg); /* free previous argument */
      args_info->directory_arg = 0;
    }
  if (args_info->kegg_arg)
    {
      free (args_info->kegg_arg); /* free previous argument */
      args_info->kegg_arg = 0;
    }
  if (args_info->kegg_org_arg)
    {
      free (args_info->kegg_org_arg); /* free previous argument */
      args_info->kegg_org_arg = 0;
    }
  if (args_info->go_onto_arg)
    {
      free (args_info->go_onto_arg); /* free previous argument */
      args_info->go_onto_arg = 0;
    }
  if (args_info->go_anno_arg)
    {
      free (args_info->go_anno_arg); /* free previous argument */
      args_info->go_anno_arg = 0;
    }
  if (args_info->go_name_arg)
    {
      free (args_info->go_name_arg); /* free previous argument */
      args_info->go_name_arg = 0;
    }
  if (args_info->mips_onto_arg)
    {
      free (args_info->mips_onto_arg); /* free previous argument */
      args_info->mips_onto_arg = 0;
    }
  if (args_info->mips_anno_arg)
    {
      free (args_info->mips_anno_arg); /* free previous argument */
      args_info->mips_anno_arg = 0;
    }
  if (args_info->sql_arg)
    {
      free (args_info->sql_arg); /* free previous argument */
      args_info->sql_arg = 0;
    }
  if (args_info->config_arg)
    {
      free (args_info->config_arg); /* free previous argument */
      args_info->config_arg = 0;
    }
  
  clear_given (args_info);
}

static int
check_possible_values(const char *val, char *values[])
{
  int i, found;
  size_t len;

  if (!val)   /* otherwise strlen() crashes below */
    return 0; /* NULL means no argument for the option */

  for (found = i = 0, len = strlen(val); values[i]; ++i)
    {
      if (strncmp(val, values[i], len) == 0)
        {
          found++;
          if (strlen(values[i]) == len)
            return 1; /* exact macth no need to check more */
        }
    }

  return found; /* return how many values are matched */
}

/* gengetopt_strdup() */
/* strdup.c replacement of strdup, which is not standard */
char *
gengetopt_strdup (const char *s)
{
  char *result = NULL;
  if (!s)
    return result;

  result = (char*)malloc(strlen(s) + 1);
  if (result == (char*)0)
    return (char*)0;
  strcpy(result, s);
  return result;
}

int
cmdline_parser (int argc, char * const *argv, struct gengetopt_args_info *args_info)
{
  return cmdline_parser2 (argc, argv, args_info, 0, 1, 1);
}

int
cmdline_parser2 (int argc, char * const *argv, struct gengetopt_args_info *args_info, int override, int initialize, int check_required)
{
  int result;

  result = cmdline_parser_internal (argc, argv, args_info, override, initialize, check_required, NULL);

  return result;
}

int
cmdline_parser_required (struct gengetopt_args_info *args_info, const char *prog_name)
{
  int result = EXIT_SUCCESS;

  if (cmdline_parser_required2(args_info, prog_name, NULL) > 0)
    result = EXIT_FAILURE;

  return result;
}

int
cmdline_parser_required2 (struct gengetopt_args_info *args_info, const char *prog_name, const char *additional_error)
{
  int error = 0;

  if (! args_info->input_given)
    {
      fprintf (stderr, "%s: '--input' ('-i') option required%s\n", prog_name, (additional_error ? additional_error : ""));
      error = 1;
    }

  return error;
}

int
cmdline_parser_internal (int argc, char * const *argv, struct gengetopt_args_info *args_info, int override, int initialize, int check_required, const char *additional_error)
{
  int c;	/* Character of the parsed option.  */

  int error = 0;
  struct gengetopt_args_info local_args_info;

  if (initialize)
    cmdline_parser_init (args_info);

  cmdline_parser_init (&local_args_info);

  optarg = 0;
  optind = 1;
  opterr = 1;
  optopt = '?';

  while (1)
    {
      int found = 0;
      int option_index = 0;
      char *stop_char;

      static struct option long_options[] = {
        { "help",	0, NULL, 'h' },
        { "version",	0, NULL, 'V' },
        { "input",	1, NULL, 'i' },
        { "output",	1, NULL, 'o' },
        { "negatives",	1, NULL, 'I' },
        { "directory",	1, NULL, 'd' },
        { "kegg",	1, NULL, 'k' },
        { "kegg_org",	1, NULL, 'K' },
        { "go_onto",	1, NULL, 'y' },
        { "go_anno",	1, NULL, 'g' },
        { "go_name",	1, NULL, 'n' },
        { "mips_onto",	1, NULL, 'm' },
        { "mips_anno",	1, NULL, 'a' },
        { "random",	1, NULL, 'r' },
        { "test",	1, NULL, 't' },
        { "synonyms",	0, NULL, 's' },
        { "sql",	1, NULL, 'q' },
        { "verbosity",	1, NULL, 'v' },
        { "config",	1, NULL, 'c' },
        { "dbids",	0, NULL, 'b' },
        { "allids",	0, NULL, 'l' },
        { "nsets",	0, NULL, 'N' },
        { "nsetlap",	1, NULL, 'L' },
        { NULL,	0, NULL, 0 }
      };

      stop_char = 0;
      c = getopt_long (argc, argv, "hVi:o:I:d:k:K:y:g:n:m:a:r:t:sq:v:c:blNL:", long_options, &option_index);

      if (c == -1) break;	/* Exit from `while (1)' loop.  */

      switch (c)
        {
        case 'h':	/* Print help and exit.  */
          cmdline_parser_print_help ();
          cmdline_parser_free (&local_args_info);
          exit (EXIT_SUCCESS);

        case 'V':	/* Print version and exit.  */
          if (local_args_info.version_given)
            {
              fprintf (stderr, "%s: `--version' (`-V') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->version_given && ! override)
            continue;
          local_args_info.version_given = 1;
          args_info->version_given = 1;
          cmdline_parser_free (&local_args_info);
          return 0;

        case 'i':	/* Ontology slim file.  */
          if (local_args_info.input_given)
            {
              fprintf (stderr, "%s: `--input' (`-i') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->input_given && ! override)
            continue;
          local_args_info.input_given = 1;
          args_info->input_given = 1;
          if (args_info->input_arg)
            free (args_info->input_arg); /* free previous string */
          args_info->input_arg = gengetopt_strdup (optarg);
          break;

        case 'o':	/* Answer file.  */
          if (local_args_info.output_given)
            {
              fprintf (stderr, "%s: `--output' (`-o') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->output_given && ! override)
            continue;
          local_args_info.output_given = 1;
          args_info->output_given = 1;
          if (args_info->output_arg)
            free (args_info->output_arg); /* free previous string */
          args_info->output_arg = gengetopt_strdup (optarg);
          break;

        case 'I':	/* Negative slim file.  */
          if (local_args_info.negatives_given)
            {
              fprintf (stderr, "%s: `--negatives' (`-I') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->negatives_given && ! override)
            continue;
          local_args_info.negatives_given = 1;
          args_info->negatives_given = 1;
          if (args_info->negatives_arg)
            free (args_info->negatives_arg); /* free previous string */
          args_info->negatives_arg = gengetopt_strdup (optarg);
          break;

        case 'd':	/* Output directory.  */
          if (local_args_info.directory_given)
            {
              fprintf (stderr, "%s: `--directory' (`-d') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->directory_given && ! override)
            continue;
          local_args_info.directory_given = 1;
          args_info->directory_given = 1;
          if (args_info->directory_arg)
            free (args_info->directory_arg); /* free previous string */
          args_info->directory_arg = gengetopt_strdup (optarg);
          break;

        case 'k':	/* KEGG ontology.  */
          if (local_args_info.kegg_given)
            {
              fprintf (stderr, "%s: `--kegg' (`-k') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->kegg_given && ! override)
            continue;
          local_args_info.kegg_given = 1;
          args_info->kegg_given = 1;
          if (args_info->kegg_arg)
            free (args_info->kegg_arg); /* free previous string */
          args_info->kegg_arg = gengetopt_strdup (optarg);
          break;

        case 'K':	/* KEGG organism.  */
          if (local_args_info.kegg_org_given)
            {
              fprintf (stderr, "%s: `--kegg_org' (`-K') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->kegg_org_given && ! override)
            continue;
          local_args_info.kegg_org_given = 1;
          args_info->kegg_org_given = 1;
          if (args_info->kegg_org_arg)
            free (args_info->kegg_org_arg); /* free previous string */
          args_info->kegg_org_arg = gengetopt_strdup (optarg);
          break;

        case 'y':	/* GO ontology.  */
          if (local_args_info.go_onto_given)
            {
              fprintf (stderr, "%s: `--go_onto' (`-y') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->go_onto_given && ! override)
            continue;
          local_args_info.go_onto_given = 1;
          args_info->go_onto_given = 1;
          if (args_info->go_onto_arg)
            free (args_info->go_onto_arg); /* free previous string */
          args_info->go_onto_arg = gengetopt_strdup (optarg);
          break;

        case 'g':	/* GO annotations.  */
          if (local_args_info.go_anno_given)
            {
              fprintf (stderr, "%s: `--go_anno' (`-g') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->go_anno_given && ! override)
            continue;
          local_args_info.go_anno_given = 1;
          args_info->go_anno_given = 1;
          if (args_info->go_anno_arg)
            free (args_info->go_anno_arg); /* free previous string */
          args_info->go_anno_arg = gengetopt_strdup (optarg);
          break;

        case 'n':	/* GO namespace.  */
          if (local_args_info.go_name_given)
            {
              fprintf (stderr, "%s: `--go_name' (`-n') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if ((found = check_possible_values(optarg, go_name_values)) != 1)
            {
              fprintf (stderr, "%s: %s argument, \"%s\", for option `--go_name' (`-n')%s\n", argv[0], found ? "ambiguous" : "invalid", optarg, (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->go_name_given && ! override)
            continue;
          local_args_info.go_name_given = 1;
          args_info->go_name_given = 1;
          if (args_info->go_name_arg)
            free (args_info->go_name_arg); /* free previous string */
          args_info->go_name_arg = gengetopt_strdup (optarg);
          break;

        case 'm':	/* MIPS ontology.  */
          if (local_args_info.mips_onto_given)
            {
              fprintf (stderr, "%s: `--mips_onto' (`-m') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->mips_onto_given && ! override)
            continue;
          local_args_info.mips_onto_given = 1;
          args_info->mips_onto_given = 1;
          if (args_info->mips_onto_arg)
            free (args_info->mips_onto_arg); /* free previous string */
          args_info->mips_onto_arg = gengetopt_strdup (optarg);
          break;

        case 'a':	/* MIPS annotations.  */
          if (local_args_info.mips_anno_given)
            {
              fprintf (stderr, "%s: `--mips_anno' (`-a') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->mips_anno_given && ! override)
            continue;
          local_args_info.mips_anno_given = 1;
          args_info->mips_anno_given = 1;
          if (args_info->mips_anno_arg)
            free (args_info->mips_anno_arg); /* free previous string */
          args_info->mips_anno_arg = gengetopt_strdup (optarg);
          break;

        case 'r':	/* Seed random generator.  */
          if (local_args_info.random_given)
            {
              fprintf (stderr, "%s: `--random' (`-r') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->random_given && ! override)
            continue;
          local_args_info.random_given = 1;
          args_info->random_given = 1;
          args_info->random_arg = strtol (optarg,&stop_char,0);
          break;

        case 't':	/* Test fraction.  */
          if (local_args_info.test_given)
            {
              fprintf (stderr, "%s: `--test' (`-t') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->test_given && ! override)
            continue;
          local_args_info.test_given = 1;
          args_info->test_given = 1;
          args_info->test_arg = strtod (optarg, NULL);
          break;

        case 's':	/* Prefer synonym names.  */
          if (local_args_info.synonyms_given)
            {
              fprintf (stderr, "%s: `--synonyms' (`-s') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->synonyms_given && ! override)
            continue;
          local_args_info.synonyms_given = 1;
          args_info->synonyms_given = 1;
          args_info->synonyms_flag = !(args_info->synonyms_flag);
          break;

        case 'q':	/* Directory in which to place SQL tables.  */
          if (local_args_info.sql_given)
            {
              fprintf (stderr, "%s: `--sql' (`-q') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->sql_given && ! override)
            continue;
          local_args_info.sql_given = 1;
          args_info->sql_given = 1;
          if (args_info->sql_arg)
            free (args_info->sql_arg); /* free previous string */
          args_info->sql_arg = gengetopt_strdup (optarg);
          break;

        case 'v':	/* Message verbosity.  */
          if (local_args_info.verbosity_given)
            {
              fprintf (stderr, "%s: `--verbosity' (`-v') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->verbosity_given && ! override)
            continue;
          local_args_info.verbosity_given = 1;
          args_info->verbosity_given = 1;
          args_info->verbosity_arg = strtol (optarg,&stop_char,0);
          break;

        case 'c':	/* Command line config file.  */
          if (local_args_info.config_given)
            {
              fprintf (stderr, "%s: `--config' (`-c') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->config_given && ! override)
            continue;
          local_args_info.config_given = 1;
          args_info->config_given = 1;
          if (args_info->config_arg)
            free (args_info->config_arg); /* free previous string */
          args_info->config_arg = gengetopt_strdup (optarg);
          break;

        case 'b':	/* Include GO database IDs.  */
          if (local_args_info.dbids_given)
            {
              fprintf (stderr, "%s: `--dbids' (`-b') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->dbids_given && ! override)
            continue;
          local_args_info.dbids_given = 1;
          args_info->dbids_given = 1;
          args_info->dbids_flag = !(args_info->dbids_flag);
          break;

        case 'l':	/* Output all available IDs.  */
          if (local_args_info.allids_given)
            {
              fprintf (stderr, "%s: `--allids' (`-l') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->allids_given && ! override)
            continue;
          local_args_info.allids_given = 1;
          args_info->allids_given = 1;
          args_info->allids_flag = !(args_info->allids_flag);
          break;

        case 'N':	/* Generate negative sets for input slim.  */
          if (local_args_info.nsets_given)
            {
              fprintf (stderr, "%s: `--nsets' (`-N') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->nsets_given && ! override)
            continue;
          local_args_info.nsets_given = 1;
          args_info->nsets_given = 1;
          args_info->nsets_flag = !(args_info->nsets_flag);
          break;

        case 'L':	/* P-value of overlap for negative rejection.  */
          if (local_args_info.nsetlap_given)
            {
              fprintf (stderr, "%s: `--nsetlap' (`-L') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->nsetlap_given && ! override)
            continue;
          local_args_info.nsetlap_given = 1;
          args_info->nsetlap_given = 1;
          args_info->nsetlap_arg = strtod (optarg, NULL);
          break;


        case 0:	/* Long option with no short option */
        case '?':	/* Invalid option.  */
          /* `getopt_long' already printed an error message.  */
          goto failure;

        default:	/* bug: option not considered.  */
          fprintf (stderr, "%s: option unknown: %c%s\n", CMDLINE_PARSER_PACKAGE, c, (additional_error ? additional_error : ""));
          abort ();
        } /* switch */
    } /* while */



  if (check_required)
    {
      error += cmdline_parser_required2 (args_info, argv[0], additional_error);
    }

  cmdline_parser_free (&local_args_info);

  if ( error )
    return (EXIT_FAILURE);

  return 0;

failure:
  
  cmdline_parser_free (&local_args_info);
  return (EXIT_FAILURE);
}

#define CONFIGPARSERBUFSIZE 256
#define ADDITIONAL_ERROR " in configuration file "

#define CONF_FILE_LINES 256
#define CONF_FILE_STRING (CONFIGPARSERBUFSIZE+3)
/* 3 is for "--" and "=" */

typedef char buffer_line_t[CONF_FILE_LINES];

buffer_line_t
my_argv[CONF_FILE_STRING];

int
cmdline_parser_configfile (char * const filename, struct gengetopt_args_info *args_info, int override, int initialize, int check_required)
{
  FILE* file;
  char linebuf[CONFIGPARSERBUFSIZE];
  int line_num = 0;
  int i, result, equal;
  char *fopt, *farg;
  char *str_index;
  size_t len, next_token;
  char delimiter;
  int my_argc = 0;
  char **my_argv_arg;
  char *additional_error;

  strcpy (my_argv[0], CMDLINE_PARSER_PACKAGE);
  for (i = 1; i < CONF_FILE_LINES; ++i)
    my_argv[i][0] = '\0';

  if ((file = fopen(filename, "r")) == NULL)
    {
      fprintf (stderr, "%s: Error opening configuration file '%s'\n",
               CMDLINE_PARSER_PACKAGE, filename);
      result = EXIT_FAILURE;
      goto conf_failure;
    }

  while ((fgets(linebuf, CONFIGPARSERBUFSIZE, file)) != NULL)
    {
      ++line_num;
      len = strlen(linebuf);
      if (len > (CONF_FILE_STRING-1))
        {
          fprintf (stderr, "%s:%s:%d: Line too long in configuration file\n",
                   CMDLINE_PARSER_PACKAGE, filename, line_num);
          result = EXIT_FAILURE;
          goto conf_failure;
        }

      /* find first non-whitespace character in the line */
      next_token = strspn ( linebuf, " \t\r\n");
      str_index  = linebuf + next_token;

      if ( str_index[0] == '\0' || str_index[0] == '#')
        continue; /* empty line or comment line is skipped */

      fopt = str_index;

      /* truncate fopt at the end of the first non-valid character */
      next_token = strcspn (fopt, " \t\r\n=");

      if (fopt[next_token] == '\0') /* the line is over */
        {
          farg  = NULL;
          equal = 0;
          goto noarg;
        }

      /* remember if equal sign is present */
      equal = (fopt[next_token] == '=');
      fopt[next_token++] = '\0';

      /* advance pointers to the next token after the end of fopt */
      next_token += strspn (fopt + next_token, " \t\r\n");
      /* check for the presence of equal sign, and if so, skip it */
      if ( !equal )
        if ((equal = (fopt[next_token] == '=')))
          {
            next_token++;
            next_token += strspn (fopt + next_token, " \t\r\n");
          }
      str_index  += next_token;

      /* find argument */
      farg = str_index;
      if ( farg[0] == '\"' || farg[0] == '\'' )
        { /* quoted argument */
          str_index = strchr (++farg, str_index[0] ); /* skip opening quote */
          if (! str_index)
            {
              fprintf
                (stderr,
                 "%s:%s:%d: unterminated string in configuration file\n",
                 CMDLINE_PARSER_PACKAGE, filename, line_num);
              result = EXIT_FAILURE;
              goto conf_failure;
            }
        }
      else
        { /* read up the remaining part up to a delimiter */
          next_token = strcspn (farg, " \t\r\n#\'\"");
          str_index += next_token;
        }

      /* truncate farg at the delimiter and store it for further check */
      delimiter = *str_index, *str_index++ = '\0';

      /* everything but comment is illegal at the end of line */
      if (delimiter != '\0' && delimiter != '#')
        {
          str_index += strspn(str_index, " \t\r\n");
          if (*str_index != '\0' && *str_index != '#')
            {
              fprintf
                (stderr,
                 "%s:%s:%d: malformed string in configuration file\n",
                 CMDLINE_PARSER_PACKAGE, filename, line_num);
              result = EXIT_FAILURE;
              goto conf_failure;
            }
        }

    noarg:
      ++my_argc;
      len = strlen(fopt);

      strcat (my_argv[my_argc], len > 1 ? "--" : "-");
      strcat (my_argv[my_argc], fopt);
      if (len > 1 && ((farg &&*farg) || equal))
            strcat (my_argv[my_argc], "=");
      if (farg && *farg)
          strcat (my_argv[my_argc], farg);
    } /* while */

  ++my_argc; /* for program name */
  my_argv_arg = (char **) malloc((my_argc+1) * sizeof(char *));
  for (i = 0; i < my_argc; ++i)
    my_argv_arg[i] = my_argv[i];
  my_argv_arg[my_argc] = 0;

  additional_error = (char *)malloc(strlen(filename) + strlen(ADDITIONAL_ERROR) + 1);
  strcpy (additional_error, ADDITIONAL_ERROR);
  strcat (additional_error, filename);
  result =
    cmdline_parser_internal (my_argc, my_argv_arg, args_info, override, initialize, check_required, additional_error);

  free (additional_error);
  free (my_argv_arg);

conf_failure:
  if (file)
    fclose(file);
  return result;
}

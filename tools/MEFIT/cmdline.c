/*
  File autogenerated by gengetopt version 2.13.1
  generated with the following command:
  ..\gengetopt-2.13.1\Release\gengetopt.exe -u -N -e -i MEFIT.ggo 

  The developers of gengetopt consider the fixed text that goes in all
  gengetopt output files to be in the public domain:
  we make no copyright claims on it.
*/

/* If we use autoconf.  */
#ifdef HAVE_CONFIG_H
#include "config.h"
#endif

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#include "getopt.h"

#include "cmdline.h"

static
void clear_given (struct gengetopt_args_info *args_info);
static
void clear_args (struct gengetopt_args_info *args_info);

static int
cmdline_parser_internal (int argc, char * const *argv, struct gengetopt_args_info *args_info, int override, int initialize, int check_required, const char *additional_error);

static int
cmdline_parser_required2 (struct gengetopt_args_info *args_info, const char *prog_name, const char *additional_error);

char *distance_values[] = {"pearson", "euclidean", "kendalls", "kolm-smir", "spearman", "pearnorm", 0} ;	/* Possible values for distance.  */

static char *
gengetopt_strdup (const char *s);

static
void clear_given (struct gengetopt_args_info *args_info)
{
  args_info->help_given = 0 ;
  args_info->version_given = 0 ;
  args_info->related_given = 0 ;
  args_info->unrelated_given = 0 ;
  args_info->output_given = 0 ;
  args_info->global_given = 0 ;
  args_info->predictions_given = 0 ;
  args_info->trusts_given = 0 ;
  args_info->bins_given = 0 ;
  args_info->distance_given = 0 ;
  args_info->genes_given = 0 ;
  args_info->genex_given = 0 ;
  args_info->random_given = 0 ;
  args_info->skip_given = 0 ;
  args_info->verbosity_given = 0 ;
  args_info->xdsl_given = 0 ;
  args_info->zero_given = 0 ;
  args_info->cutoff_given = 0 ;
}

static
void clear_args (struct gengetopt_args_info *args_info)
{
  args_info->related_arg = NULL;
  args_info->unrelated_arg = NULL;
  args_info->output_arg = NULL;
  args_info->global_arg = NULL;
  args_info->predictions_arg = NULL;
  args_info->trusts_arg = NULL;
  args_info->bins_arg = NULL;
  args_info->distance_arg = gengetopt_strdup ("pearnorm");
  args_info->genes_arg = NULL;
  args_info->genex_arg = NULL;
  args_info->random_arg = 0 ;
  args_info->skip_arg = 2 ;
  args_info->verbosity_arg = 5 ;
  args_info->xdsl_flag = 1;
  args_info->zero_flag = 0;
  args_info->cutoff_arg = 0 ;
}

void
cmdline_parser_print_version (void)
{
  printf ("%s %s\n", CMDLINE_PARSER_PACKAGE, CMDLINE_PARSER_VERSION);
}

void
cmdline_parser_print_help (void)
{
  cmdline_parser_print_version ();
  printf("\n%s\n", "Microarray Expression Functional Integration Technique (Huttenhower et al,\n	Bioinformatics 2006)\n\nMEFIT takes as input:\n1. A collection of microarray data sets (PCL files provided on the command \nline)\n2. A collection of known biological functions (lists of related genes provided\n   using the -r flag)\n3. A collection of known unrelated gene pairs (provided using the -u flag)\n\nIt produces as output:\n1. A global Bayesian network learned by considered all of the data sets\n   independently of biological function (specified using the -O flag)\n2. One Bayesian network per biological function (placed in the directory\n   specified by the -o flag)\n3. Predicted probabilities of functional relationships within each biological\n   function of interest (placed in the directory specified by the -p flag)\n4. Trust scores for each input data set and function indicating how\n   predictive a data set is within a function (specified by the -t flag)");
  printf("\nUsage: MEFIT [OPTIONS]... [FILES]...\n\n");
  printf("%s\n","  -h, --help                   Print help and exit");
  printf("%s\n","  -V, --version                Print version and exit");
  printf("%s\n","\nInputs:");
  printf("%s\n","  -r, --related=directory      Directory containing lists of known related \n                                 genes");
  printf("%s\n","  -u, --unrelated=filename     List of known unrelated gene pairs");
  printf("%s\n","\nOutputs:");
  printf("%s\n","  -o, --output=directory       Directory to contain learned per-function \n                                 Bayesian networks");
  printf("%s\n","  -O, --global=filename        Global learned Bayesian network");
  printf("%s\n","  -p, --predictions=directory  Directory to contain predicted probabilities of \n                                 functional relationship");
  printf("%s\n","  -t, --trusts=filename        Trust scores learned per data set and function");
  printf("%s\n","\nOptional:");
  printf("%s\n","  -b, --bins=filename          Tab separated bin cutoffs");
  printf("%s\n","  -d, --distance=STRING        Distance measure  (possible values=\"pearson\", \n                                 \"euclidean\", \"kendalls\", \"kolm-smir\", \n                                 \"spearman\", \"pearnorm\" default=`pearnorm')");
  printf("%s\n","  -g, --genes=filename         Subset of genes to include in evaluation");
  printf("%s\n","  -G, --genex=filename         Subset of genes to exclude from evaluation");
  printf("%s\n","  -R, --random=INT             Seed random generator  (default=`0')");
  printf("%s\n","  -s, --skip=INT               Additional columns to skip in input PCLs  \n                                 (default=`2')");
  printf("%s\n","  -v, --verbosity=INT          Message verbosity  (default=`5')");
  printf("%s\n","  -x, --xdsl                   Output .xdsl files in place of .dsls  \n                                 (default=on)");
  printf("%s\n","  -z, --zero                   Zero missing values  (default=off)");
  printf("%s\n","  -c, --cutoff=DOUBLE          Include only confidences above cutoff  \n                                 (default=`0')");
  
}

void
cmdline_parser_init (struct gengetopt_args_info *args_info)
{
  clear_given (args_info);
  clear_args (args_info);

  args_info->inputs = NULL;
  args_info->inputs_num = 0;
}

void
cmdline_parser_free (struct gengetopt_args_info *args_info)
{
  
  unsigned int i;
  if (args_info->related_arg)
    {
      free (args_info->related_arg); /* free previous argument */
      args_info->related_arg = 0;
    }
  if (args_info->unrelated_arg)
    {
      free (args_info->unrelated_arg); /* free previous argument */
      args_info->unrelated_arg = 0;
    }
  if (args_info->output_arg)
    {
      free (args_info->output_arg); /* free previous argument */
      args_info->output_arg = 0;
    }
  if (args_info->global_arg)
    {
      free (args_info->global_arg); /* free previous argument */
      args_info->global_arg = 0;
    }
  if (args_info->predictions_arg)
    {
      free (args_info->predictions_arg); /* free previous argument */
      args_info->predictions_arg = 0;
    }
  if (args_info->trusts_arg)
    {
      free (args_info->trusts_arg); /* free previous argument */
      args_info->trusts_arg = 0;
    }
  if (args_info->bins_arg)
    {
      free (args_info->bins_arg); /* free previous argument */
      args_info->bins_arg = 0;
    }
  if (args_info->distance_arg)
    {
      free (args_info->distance_arg); /* free previous argument */
      args_info->distance_arg = 0;
    }
  if (args_info->genes_arg)
    {
      free (args_info->genes_arg); /* free previous argument */
      args_info->genes_arg = 0;
    }
  if (args_info->genex_arg)
    {
      free (args_info->genex_arg); /* free previous argument */
      args_info->genex_arg = 0;
    }
  
  for (i = 0; i < args_info->inputs_num; ++i)
    free (args_info->inputs [i]);
  
  if (args_info->inputs_num)
    free (args_info->inputs);
  
  clear_given (args_info);
}

static int
check_possible_values(const char *val, char *values[])
{
  int i, found;
  size_t len;

  if (!val)   /* otherwise strlen() crashes below */
    return 0; /* NULL means no argument for the option */

  for (found = i = 0, len = strlen(val); values[i]; ++i)
    {
      if (strncmp(val, values[i], len) == 0)
        {
          found++;
          if (strlen(values[i]) == len)
            return 1; /* exact macth no need to check more */
        }
    }

  return found; /* return how many values are matched */
}

/* gengetopt_strdup() */
/* strdup.c replacement of strdup, which is not standard */
char *
gengetopt_strdup (const char *s)
{
  char *result = NULL;
  if (!s)
    return result;

  result = (char*)malloc(strlen(s) + 1);
  if (result == (char*)0)
    return (char*)0;
  strcpy(result, s);
  return result;
}

int
cmdline_parser (int argc, char * const *argv, struct gengetopt_args_info *args_info)
{
  return cmdline_parser2 (argc, argv, args_info, 0, 1, 1);
}

int
cmdline_parser2 (int argc, char * const *argv, struct gengetopt_args_info *args_info, int override, int initialize, int check_required)
{
  int result;

  result = cmdline_parser_internal (argc, argv, args_info, override, initialize, check_required, NULL);

  return result;
}

int
cmdline_parser_required (struct gengetopt_args_info *args_info, const char *prog_name)
{
  int result = EXIT_SUCCESS;

  if (cmdline_parser_required2(args_info, prog_name, NULL) > 0)
    result = EXIT_FAILURE;

  return result;
}

int
cmdline_parser_required2 (struct gengetopt_args_info *args_info, const char *prog_name, const char *additional_error)
{
  int error = 0;

  if (! args_info->related_given)
    {
      fprintf (stderr, "%s: '--related' ('-r') option required%s\n", prog_name, (additional_error ? additional_error : ""));
      error = 1;
    }
  if (! args_info->unrelated_given)
    {
      fprintf (stderr, "%s: '--unrelated' ('-u') option required%s\n", prog_name, (additional_error ? additional_error : ""));
      error = 1;
    }
  if (! args_info->output_given)
    {
      fprintf (stderr, "%s: '--output' ('-o') option required%s\n", prog_name, (additional_error ? additional_error : ""));
      error = 1;
    }
  if (! args_info->global_given)
    {
      fprintf (stderr, "%s: '--global' ('-O') option required%s\n", prog_name, (additional_error ? additional_error : ""));
      error = 1;
    }
  if (! args_info->predictions_given)
    {
      fprintf (stderr, "%s: '--predictions' ('-p') option required%s\n", prog_name, (additional_error ? additional_error : ""));
      error = 1;
    }
  if (! args_info->trusts_given)
    {
      fprintf (stderr, "%s: '--trusts' ('-t') option required%s\n", prog_name, (additional_error ? additional_error : ""));
      error = 1;
    }

  return error;
}

int
cmdline_parser_internal (int argc, char * const *argv, struct gengetopt_args_info *args_info, int override, int initialize, int check_required, const char *additional_error)
{
  int c;	/* Character of the parsed option.  */

  int error = 0;
  struct gengetopt_args_info local_args_info;

  if (initialize)
    cmdline_parser_init (args_info);

  cmdline_parser_init (&local_args_info);

  optarg = 0;
  optind = 1;
  opterr = 1;
  optopt = '?';

  while (1)
    {
      int found = 0;
      int option_index = 0;
      char *stop_char;

      static struct option long_options[] = {
        { "help",	0, NULL, 'h' },
        { "version",	0, NULL, 'V' },
        { "related",	1, NULL, 'r' },
        { "unrelated",	1, NULL, 'u' },
        { "output",	1, NULL, 'o' },
        { "global",	1, NULL, 'O' },
        { "predictions",	1, NULL, 'p' },
        { "trusts",	1, NULL, 't' },
        { "bins",	1, NULL, 'b' },
        { "distance",	1, NULL, 'd' },
        { "genes",	1, NULL, 'g' },
        { "genex",	1, NULL, 'G' },
        { "random",	1, NULL, 'R' },
        { "skip",	1, NULL, 's' },
        { "verbosity",	1, NULL, 'v' },
        { "xdsl",	0, NULL, 'x' },
        { "zero",	0, NULL, 'z' },
        { "cutoff",	1, NULL, 'c' },
        { NULL,	0, NULL, 0 }
      };

      stop_char = 0;
      c = getopt_long (argc, argv, "hVr:u:o:O:p:t:b:d:g:G:R:s:v:xzc:", long_options, &option_index);

      if (c == -1) break;	/* Exit from `while (1)' loop.  */

      switch (c)
        {
        case 'h':	/* Print help and exit.  */
          cmdline_parser_print_help ();
          cmdline_parser_free (&local_args_info);
          exit (EXIT_SUCCESS);

        case 'V':	/* Print version and exit.  */
          if (local_args_info.version_given)
            {
              fprintf (stderr, "%s: `--version' (`-V') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->version_given && ! override)
            continue;
          local_args_info.version_given = 1;
          args_info->version_given = 1;
          cmdline_parser_free (&local_args_info);
          return 0;

        case 'r':	/* Directory containing lists of known related genes.  */
          if (local_args_info.related_given)
            {
              fprintf (stderr, "%s: `--related' (`-r') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->related_given && ! override)
            continue;
          local_args_info.related_given = 1;
          args_info->related_given = 1;
          if (args_info->related_arg)
            free (args_info->related_arg); /* free previous string */
          args_info->related_arg = gengetopt_strdup (optarg);
          break;

        case 'u':	/* List of known unrelated gene pairs.  */
          if (local_args_info.unrelated_given)
            {
              fprintf (stderr, "%s: `--unrelated' (`-u') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->unrelated_given && ! override)
            continue;
          local_args_info.unrelated_given = 1;
          args_info->unrelated_given = 1;
          if (args_info->unrelated_arg)
            free (args_info->unrelated_arg); /* free previous string */
          args_info->unrelated_arg = gengetopt_strdup (optarg);
          break;

        case 'o':	/* Directory to contain learned per-function Bayesian networks.  */
          if (local_args_info.output_given)
            {
              fprintf (stderr, "%s: `--output' (`-o') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->output_given && ! override)
            continue;
          local_args_info.output_given = 1;
          args_info->output_given = 1;
          if (args_info->output_arg)
            free (args_info->output_arg); /* free previous string */
          args_info->output_arg = gengetopt_strdup (optarg);
          break;

        case 'O':	/* Global learned Bayesian network.  */
          if (local_args_info.global_given)
            {
              fprintf (stderr, "%s: `--global' (`-O') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->global_given && ! override)
            continue;
          local_args_info.global_given = 1;
          args_info->global_given = 1;
          if (args_info->global_arg)
            free (args_info->global_arg); /* free previous string */
          args_info->global_arg = gengetopt_strdup (optarg);
          break;

        case 'p':	/* Directory to contain predicted probabilities of functional relationship.  */
          if (local_args_info.predictions_given)
            {
              fprintf (stderr, "%s: `--predictions' (`-p') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->predictions_given && ! override)
            continue;
          local_args_info.predictions_given = 1;
          args_info->predictions_given = 1;
          if (args_info->predictions_arg)
            free (args_info->predictions_arg); /* free previous string */
          args_info->predictions_arg = gengetopt_strdup (optarg);
          break;

        case 't':	/* Trust scores learned per data set and function.  */
          if (local_args_info.trusts_given)
            {
              fprintf (stderr, "%s: `--trusts' (`-t') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->trusts_given && ! override)
            continue;
          local_args_info.trusts_given = 1;
          args_info->trusts_given = 1;
          if (args_info->trusts_arg)
            free (args_info->trusts_arg); /* free previous string */
          args_info->trusts_arg = gengetopt_strdup (optarg);
          break;

        case 'b':	/* Tab separated bin cutoffs.  */
          if (local_args_info.bins_given)
            {
              fprintf (stderr, "%s: `--bins' (`-b') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->bins_given && ! override)
            continue;
          local_args_info.bins_given = 1;
          args_info->bins_given = 1;
          if (args_info->bins_arg)
            free (args_info->bins_arg); /* free previous string */
          args_info->bins_arg = gengetopt_strdup (optarg);
          break;

        case 'd':	/* Distance measure.  */
          if (local_args_info.distance_given)
            {
              fprintf (stderr, "%s: `--distance' (`-d') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if ((found = check_possible_values(optarg, distance_values)) != 1)
            {
              fprintf (stderr, "%s: %s argument, \"%s\", for option `--distance' (`-d')%s\n", argv[0], found ? "ambiguous" : "invalid", optarg, (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->distance_given && ! override)
            continue;
          local_args_info.distance_given = 1;
          args_info->distance_given = 1;
          if (args_info->distance_arg)
            free (args_info->distance_arg); /* free previous string */
          args_info->distance_arg = gengetopt_strdup (optarg);
          break;

        case 'g':	/* Subset of genes to include in evaluation.  */
          if (local_args_info.genes_given)
            {
              fprintf (stderr, "%s: `--genes' (`-g') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->genes_given && ! override)
            continue;
          local_args_info.genes_given = 1;
          args_info->genes_given = 1;
          if (args_info->genes_arg)
            free (args_info->genes_arg); /* free previous string */
          args_info->genes_arg = gengetopt_strdup (optarg);
          break;

        case 'G':	/* Subset of genes to exclude from evaluation.  */
          if (local_args_info.genex_given)
            {
              fprintf (stderr, "%s: `--genex' (`-G') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->genex_given && ! override)
            continue;
          local_args_info.genex_given = 1;
          args_info->genex_given = 1;
          if (args_info->genex_arg)
            free (args_info->genex_arg); /* free previous string */
          args_info->genex_arg = gengetopt_strdup (optarg);
          break;

        case 'R':	/* Seed random generator.  */
          if (local_args_info.random_given)
            {
              fprintf (stderr, "%s: `--random' (`-R') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->random_given && ! override)
            continue;
          local_args_info.random_given = 1;
          args_info->random_given = 1;
          args_info->random_arg = strtol (optarg,&stop_char,0);
          break;

        case 's':	/* Additional columns to skip in input PCLs.  */
          if (local_args_info.skip_given)
            {
              fprintf (stderr, "%s: `--skip' (`-s') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->skip_given && ! override)
            continue;
          local_args_info.skip_given = 1;
          args_info->skip_given = 1;
          args_info->skip_arg = strtol (optarg,&stop_char,0);
          break;

        case 'v':	/* Message verbosity.  */
          if (local_args_info.verbosity_given)
            {
              fprintf (stderr, "%s: `--verbosity' (`-v') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->verbosity_given && ! override)
            continue;
          local_args_info.verbosity_given = 1;
          args_info->verbosity_given = 1;
          args_info->verbosity_arg = strtol (optarg,&stop_char,0);
          break;

        case 'x':	/* Output .xdsl files in place of .dsls.  */
          if (local_args_info.xdsl_given)
            {
              fprintf (stderr, "%s: `--xdsl' (`-x') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->xdsl_given && ! override)
            continue;
          local_args_info.xdsl_given = 1;
          args_info->xdsl_given = 1;
          args_info->xdsl_flag = !(args_info->xdsl_flag);
          break;

        case 'z':	/* Zero missing values.  */
          if (local_args_info.zero_given)
            {
              fprintf (stderr, "%s: `--zero' (`-z') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->zero_given && ! override)
            continue;
          local_args_info.zero_given = 1;
          args_info->zero_given = 1;
          args_info->zero_flag = !(args_info->zero_flag);
          break;

        case 'c':	/* Include only confidences above cutoff.  */
          if (local_args_info.cutoff_given)
            {
              fprintf (stderr, "%s: `--cutoff' (`-c') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->cutoff_given && ! override)
            continue;
          local_args_info.cutoff_given = 1;
          args_info->cutoff_given = 1;
          args_info->cutoff_arg = strtod (optarg, NULL);
          break;


        case 0:	/* Long option with no short option */
        case '?':	/* Invalid option.  */
          /* `getopt_long' already printed an error message.  */
          goto failure;

        default:	/* bug: option not considered.  */
          fprintf (stderr, "%s: option unknown: %c%s\n", CMDLINE_PARSER_PACKAGE, c, (additional_error ? additional_error : ""));
          abort ();
        } /* switch */
    } /* while */



  if (check_required)
    {
      error += cmdline_parser_required2 (args_info, argv[0], additional_error);
    }

  cmdline_parser_free (&local_args_info);

  if ( error )
    return (EXIT_FAILURE);

  if (optind < argc)
    {
      int i = 0 ;

      args_info->inputs_num = argc - optind ;
      args_info->inputs =
        (char **)(malloc ((args_info->inputs_num)*sizeof(char *))) ;
      while (optind < argc)
        args_info->inputs[ i++ ] = gengetopt_strdup (argv[optind++]) ;
    }

  return 0;

failure:
  
  cmdline_parser_free (&local_args_info);
  return (EXIT_FAILURE);
}

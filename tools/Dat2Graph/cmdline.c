/*
  File autogenerated by gengetopt version 2.13.1
  generated with the following command:
  ..\gengetopt-2.13.1\Release\gengetopt.exe -C -N -e -i Dat2Graph.ggo 

  The developers of gengetopt consider the fixed text that goes in all
  gengetopt output files to be in the public domain:
  we make no copyright claims on it.
*/

/* If we use autoconf.  */
#ifdef HAVE_CONFIG_H
#include "config.h"
#endif

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#include "getopt.h"

#include "cmdline.h"

static
void clear_given (struct gengetopt_args_info *args_info);
static
void clear_args (struct gengetopt_args_info *args_info);

static int
cmdline_parser_internal (int argc, char * const *argv, struct gengetopt_args_info *args_info, int override, int initialize, int check_required, const char *additional_error);


char *format_values[] = {"dot", "gdf", "net", "matisse", "list", "dat", "correl", 0} ;	/* Possible values for format.  */

static char *
gengetopt_strdup (const char *s);

static
void clear_given (struct gengetopt_args_info *args_info)
{
  args_info->help_given = 0 ;
  args_info->version_given = 0 ;
  args_info->input_given = 0 ;
  args_info->format_given = 0 ;
  args_info->cutoff_given = 0 ;
  args_info->features_given = 0 ;
  args_info->verbosity_given = 0 ;
  args_info->config_given = 0 ;
  args_info->memmap_given = 0 ;
  args_info->normalize_given = 0 ;
  args_info->neighbors_given = 0 ;
  args_info->genes_given = 0 ;
  args_info->geneq_given = 0 ;
  args_info->knowns_given = 0 ;
  args_info->colors_given = 0 ;
  args_info->borders_given = 0 ;
}

static
void clear_args (struct gengetopt_args_info *args_info)
{
  args_info->input_arg = NULL;
  args_info->format_arg = gengetopt_strdup ("dot");
  args_info->features_arg = NULL;
  args_info->verbosity_arg = 5 ;
  args_info->config_arg = gengetopt_strdup ("Dat2Graph.ini");
  args_info->memmap_flag = 0;
  args_info->normalize_flag = 0;
  args_info->neighbors_arg = -1 ;
  args_info->genes_arg = NULL;
  args_info->geneq_arg = NULL;
  args_info->knowns_arg = NULL;
  args_info->colors_arg = NULL;
  args_info->borders_arg = NULL;
}

void
cmdline_parser_print_version (void)
{
  printf ("%s %s\n", CMDLINE_PARSER_PACKAGE, CMDLINE_PARSER_VERSION);
}

void
cmdline_parser_print_help (void)
{
  cmdline_parser_print_version ();
  printf("\n%s\n", "Text/binary data file graph output");
  printf("\nUsage: Dat2Graph [OPTIONS]...\n\n");
  printf("%s\n","  -h, --help               Print help and exit");
  printf("%s\n","  -V, --version            Print version and exit");
  printf("%s\n","\nOptional:");
  printf("%s\n","  -i, --input=filename     Input dab file");
  printf("%s\n","  -t, --format=STRING      Output graph format  (possible values=\"dot\", \n                             \"gdf\", \"net\", \"matisse\", \"list\", \"dat\", \n                             \"correl\" default=`dot')");
  printf("%s\n","  -e, --cutoff=DOUBLE      Minimum edge weight for output");
  printf("%s\n","  -f, --features=filename  SGD gene features");
  printf("%s\n","  -v, --verbosity=INT      Message verbosity  (default=`5')");
  printf("%s\n","  -c, --config=filename    Command line config file  (default=`Dat2Graph.ini')");
  printf("%s\n","  -m, --memmap             Memory map input file  (default=off)");
  printf("%s\n","  -n, --normalize          Normalize edge weights before processing  \n                             (default=off)");
  printf("%s\n","  -k, --neighbors=INT      Size of Pixie neighborhood  (default=`-1')");
  printf("%s\n","  -g, --genes=filename     Gene inclusion file");
  printf("%s\n","  -q, --geneq=filename     Query inclusion file");
  printf("%s\n","  -w, --knowns=filename    Known interactions (DAT) to ignore");
  printf("%s\n","  -l, --colors=filename    Colors for graph nodes");
  printf("%s\n","  -b, --borders=filename   Borders for graph nodes");
  
}

void
cmdline_parser_init (struct gengetopt_args_info *args_info)
{
  clear_given (args_info);
  clear_args (args_info);
}

void
cmdline_parser_free (struct gengetopt_args_info *args_info)
{
  
  if (args_info->input_arg)
    {
      free (args_info->input_arg); /* free previous argument */
      args_info->input_arg = 0;
    }
  if (args_info->format_arg)
    {
      free (args_info->format_arg); /* free previous argument */
      args_info->format_arg = 0;
    }
  if (args_info->features_arg)
    {
      free (args_info->features_arg); /* free previous argument */
      args_info->features_arg = 0;
    }
  if (args_info->config_arg)
    {
      free (args_info->config_arg); /* free previous argument */
      args_info->config_arg = 0;
    }
  if (args_info->genes_arg)
    {
      free (args_info->genes_arg); /* free previous argument */
      args_info->genes_arg = 0;
    }
  if (args_info->geneq_arg)
    {
      free (args_info->geneq_arg); /* free previous argument */
      args_info->geneq_arg = 0;
    }
  if (args_info->knowns_arg)
    {
      free (args_info->knowns_arg); /* free previous argument */
      args_info->knowns_arg = 0;
    }
  if (args_info->colors_arg)
    {
      free (args_info->colors_arg); /* free previous argument */
      args_info->colors_arg = 0;
    }
  if (args_info->borders_arg)
    {
      free (args_info->borders_arg); /* free previous argument */
      args_info->borders_arg = 0;
    }
  
  clear_given (args_info);
}

static int
check_possible_values(const char *val, char *values[])
{
  int i, found;
  size_t len;

  if (!val)   /* otherwise strlen() crashes below */
    return 0; /* NULL means no argument for the option */

  for (found = i = 0, len = strlen(val); values[i]; ++i)
    {
      if (strncmp(val, values[i], len) == 0)
        {
          found++;
          if (strlen(values[i]) == len)
            return 1; /* exact macth no need to check more */
        }
    }

  return found; /* return how many values are matched */
}

/* gengetopt_strdup() */
/* strdup.c replacement of strdup, which is not standard */
char *
gengetopt_strdup (const char *s)
{
  char *result = NULL;
  if (!s)
    return result;

  result = (char*)malloc(strlen(s) + 1);
  if (result == (char*)0)
    return (char*)0;
  strcpy(result, s);
  return result;
}

int
cmdline_parser (int argc, char * const *argv, struct gengetopt_args_info *args_info)
{
  return cmdline_parser2 (argc, argv, args_info, 0, 1, 1);
}

int
cmdline_parser2 (int argc, char * const *argv, struct gengetopt_args_info *args_info, int override, int initialize, int check_required)
{
  int result;

  result = cmdline_parser_internal (argc, argv, args_info, override, initialize, check_required, NULL);

  return result;
}

int
cmdline_parser_required (struct gengetopt_args_info *args_info, const char *prog_name)
{
  return EXIT_SUCCESS;
}

int
cmdline_parser_internal (int argc, char * const *argv, struct gengetopt_args_info *args_info, int override, int initialize, int check_required, const char *additional_error)
{
  int c;	/* Character of the parsed option.  */

  int error = 0;
  struct gengetopt_args_info local_args_info;

  if (initialize)
    cmdline_parser_init (args_info);

  cmdline_parser_init (&local_args_info);

  optarg = 0;
  optind = 1;
  opterr = 1;
  optopt = '?';

  while (1)
    {
      int found = 0;
      int option_index = 0;
      char *stop_char;

      static struct option long_options[] = {
        { "help",	0, NULL, 'h' },
        { "version",	0, NULL, 'V' },
        { "input",	1, NULL, 'i' },
        { "format",	1, NULL, 't' },
        { "cutoff",	1, NULL, 'e' },
        { "features",	1, NULL, 'f' },
        { "verbosity",	1, NULL, 'v' },
        { "config",	1, NULL, 'c' },
        { "memmap",	0, NULL, 'm' },
        { "normalize",	0, NULL, 'n' },
        { "neighbors",	1, NULL, 'k' },
        { "genes",	1, NULL, 'g' },
        { "geneq",	1, NULL, 'q' },
        { "knowns",	1, NULL, 'w' },
        { "colors",	1, NULL, 'l' },
        { "borders",	1, NULL, 'b' },
        { NULL,	0, NULL, 0 }
      };

      stop_char = 0;
      c = getopt_long (argc, argv, "hVi:t:e:f:v:c:mnk:g:q:w:l:b:", long_options, &option_index);

      if (c == -1) break;	/* Exit from `while (1)' loop.  */

      switch (c)
        {
        case 'h':	/* Print help and exit.  */
          cmdline_parser_print_help ();
          cmdline_parser_free (&local_args_info);
          exit (EXIT_SUCCESS);

        case 'V':	/* Print version and exit.  */
          if (local_args_info.version_given)
            {
              fprintf (stderr, "%s: `--version' (`-V') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->version_given && ! override)
            continue;
          local_args_info.version_given = 1;
          args_info->version_given = 1;
          cmdline_parser_free (&local_args_info);
          return 0;

        case 'i':	/* Input dab file.  */
          if (local_args_info.input_given)
            {
              fprintf (stderr, "%s: `--input' (`-i') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->input_given && ! override)
            continue;
          local_args_info.input_given = 1;
          args_info->input_given = 1;
          if (args_info->input_arg)
            free (args_info->input_arg); /* free previous string */
          args_info->input_arg = gengetopt_strdup (optarg);
          break;

        case 't':	/* Output graph format.  */
          if (local_args_info.format_given)
            {
              fprintf (stderr, "%s: `--format' (`-t') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if ((found = check_possible_values(optarg, format_values)) != 1)
            {
              fprintf (stderr, "%s: %s argument, \"%s\", for option `--format' (`-t')%s\n", argv[0], found ? "ambiguous" : "invalid", optarg, (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->format_given && ! override)
            continue;
          local_args_info.format_given = 1;
          args_info->format_given = 1;
          if (args_info->format_arg)
            free (args_info->format_arg); /* free previous string */
          args_info->format_arg = gengetopt_strdup (optarg);
          break;

        case 'e':	/* Minimum edge weight for output.  */
          if (local_args_info.cutoff_given)
            {
              fprintf (stderr, "%s: `--cutoff' (`-e') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->cutoff_given && ! override)
            continue;
          local_args_info.cutoff_given = 1;
          args_info->cutoff_given = 1;
          args_info->cutoff_arg = strtod (optarg, NULL);
          break;

        case 'f':	/* SGD gene features.  */
          if (local_args_info.features_given)
            {
              fprintf (stderr, "%s: `--features' (`-f') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->features_given && ! override)
            continue;
          local_args_info.features_given = 1;
          args_info->features_given = 1;
          if (args_info->features_arg)
            free (args_info->features_arg); /* free previous string */
          args_info->features_arg = gengetopt_strdup (optarg);
          break;

        case 'v':	/* Message verbosity.  */
          if (local_args_info.verbosity_given)
            {
              fprintf (stderr, "%s: `--verbosity' (`-v') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->verbosity_given && ! override)
            continue;
          local_args_info.verbosity_given = 1;
          args_info->verbosity_given = 1;
          args_info->verbosity_arg = strtol (optarg,&stop_char,0);
          break;

        case 'c':	/* Command line config file.  */
          if (local_args_info.config_given)
            {
              fprintf (stderr, "%s: `--config' (`-c') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->config_given && ! override)
            continue;
          local_args_info.config_given = 1;
          args_info->config_given = 1;
          if (args_info->config_arg)
            free (args_info->config_arg); /* free previous string */
          args_info->config_arg = gengetopt_strdup (optarg);
          break;

        case 'm':	/* Memory map input file.  */
          if (local_args_info.memmap_given)
            {
              fprintf (stderr, "%s: `--memmap' (`-m') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->memmap_given && ! override)
            continue;
          local_args_info.memmap_given = 1;
          args_info->memmap_given = 1;
          args_info->memmap_flag = !(args_info->memmap_flag);
          break;

        case 'n':	/* Normalize edge weights before processing.  */
          if (local_args_info.normalize_given)
            {
              fprintf (stderr, "%s: `--normalize' (`-n') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->normalize_given && ! override)
            continue;
          local_args_info.normalize_given = 1;
          args_info->normalize_given = 1;
          args_info->normalize_flag = !(args_info->normalize_flag);
          break;

        case 'k':	/* Size of Pixie neighborhood.  */
          if (local_args_info.neighbors_given)
            {
              fprintf (stderr, "%s: `--neighbors' (`-k') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->neighbors_given && ! override)
            continue;
          local_args_info.neighbors_given = 1;
          args_info->neighbors_given = 1;
          args_info->neighbors_arg = strtol (optarg,&stop_char,0);
          break;

        case 'g':	/* Gene inclusion file.  */
          if (local_args_info.genes_given)
            {
              fprintf (stderr, "%s: `--genes' (`-g') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->genes_given && ! override)
            continue;
          local_args_info.genes_given = 1;
          args_info->genes_given = 1;
          if (args_info->genes_arg)
            free (args_info->genes_arg); /* free previous string */
          args_info->genes_arg = gengetopt_strdup (optarg);
          break;

        case 'q':	/* Query inclusion file.  */
          if (local_args_info.geneq_given)
            {
              fprintf (stderr, "%s: `--geneq' (`-q') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->geneq_given && ! override)
            continue;
          local_args_info.geneq_given = 1;
          args_info->geneq_given = 1;
          if (args_info->geneq_arg)
            free (args_info->geneq_arg); /* free previous string */
          args_info->geneq_arg = gengetopt_strdup (optarg);
          break;

        case 'w':	/* Known interactions (DAT) to ignore.  */
          if (local_args_info.knowns_given)
            {
              fprintf (stderr, "%s: `--knowns' (`-w') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->knowns_given && ! override)
            continue;
          local_args_info.knowns_given = 1;
          args_info->knowns_given = 1;
          if (args_info->knowns_arg)
            free (args_info->knowns_arg); /* free previous string */
          args_info->knowns_arg = gengetopt_strdup (optarg);
          break;

        case 'l':	/* Colors for graph nodes.  */
          if (local_args_info.colors_given)
            {
              fprintf (stderr, "%s: `--colors' (`-l') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->colors_given && ! override)
            continue;
          local_args_info.colors_given = 1;
          args_info->colors_given = 1;
          if (args_info->colors_arg)
            free (args_info->colors_arg); /* free previous string */
          args_info->colors_arg = gengetopt_strdup (optarg);
          break;

        case 'b':	/* Borders for graph nodes.  */
          if (local_args_info.borders_given)
            {
              fprintf (stderr, "%s: `--borders' (`-b') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->borders_given && ! override)
            continue;
          local_args_info.borders_given = 1;
          args_info->borders_given = 1;
          if (args_info->borders_arg)
            free (args_info->borders_arg); /* free previous string */
          args_info->borders_arg = gengetopt_strdup (optarg);
          break;


        case 0:	/* Long option with no short option */
        case '?':	/* Invalid option.  */
          /* `getopt_long' already printed an error message.  */
          goto failure;

        default:	/* bug: option not considered.  */
          fprintf (stderr, "%s: option unknown: %c%s\n", CMDLINE_PARSER_PACKAGE, c, (additional_error ? additional_error : ""));
          abort ();
        } /* switch */
    } /* while */




  cmdline_parser_free (&local_args_info);

  if ( error )
    return (EXIT_FAILURE);

  return 0;

failure:
  
  cmdline_parser_free (&local_args_info);
  return (EXIT_FAILURE);
}

#define CONFIGPARSERBUFSIZE 256
#define ADDITIONAL_ERROR " in configuration file "

#define CONF_FILE_LINES 256
#define CONF_FILE_STRING (CONFIGPARSERBUFSIZE+3)
/* 3 is for "--" and "=" */

typedef char buffer_line_t[CONF_FILE_LINES];

buffer_line_t
my_argv[CONF_FILE_STRING];

int
cmdline_parser_configfile (char * const filename, struct gengetopt_args_info *args_info, int override, int initialize, int check_required)
{
  FILE* file;
  char linebuf[CONFIGPARSERBUFSIZE];
  int line_num = 0;
  int i, result, equal;
  char *fopt, *farg;
  char *str_index;
  size_t len, next_token;
  char delimiter;
  int my_argc = 0;
  char **my_argv_arg;
  char *additional_error;

  strcpy (my_argv[0], CMDLINE_PARSER_PACKAGE);
  for (i = 1; i < CONF_FILE_LINES; ++i)
    my_argv[i][0] = '\0';

  if ((file = fopen(filename, "r")) == NULL)
    {
      fprintf (stderr, "%s: Error opening configuration file '%s'\n",
               CMDLINE_PARSER_PACKAGE, filename);
      result = EXIT_FAILURE;
      goto conf_failure;
    }

  while ((fgets(linebuf, CONFIGPARSERBUFSIZE, file)) != NULL)
    {
      ++line_num;
      len = strlen(linebuf);
      if (len > (CONF_FILE_STRING-1))
        {
          fprintf (stderr, "%s:%s:%d: Line too long in configuration file\n",
                   CMDLINE_PARSER_PACKAGE, filename, line_num);
          result = EXIT_FAILURE;
          goto conf_failure;
        }

      /* find first non-whitespace character in the line */
      next_token = strspn ( linebuf, " \t\r\n");
      str_index  = linebuf + next_token;

      if ( str_index[0] == '\0' || str_index[0] == '#')
        continue; /* empty line or comment line is skipped */

      fopt = str_index;

      /* truncate fopt at the end of the first non-valid character */
      next_token = strcspn (fopt, " \t\r\n=");

      if (fopt[next_token] == '\0') /* the line is over */
        {
          farg  = NULL;
          equal = 0;
          goto noarg;
        }

      /* remember if equal sign is present */
      equal = (fopt[next_token] == '=');
      fopt[next_token++] = '\0';

      /* advance pointers to the next token after the end of fopt */
      next_token += strspn (fopt + next_token, " \t\r\n");
      /* check for the presence of equal sign, and if so, skip it */
      if ( !equal )
        if ((equal = (fopt[next_token] == '=')))
          {
            next_token++;
            next_token += strspn (fopt + next_token, " \t\r\n");
          }
      str_index  += next_token;

      /* find argument */
      farg = str_index;
      if ( farg[0] == '\"' || farg[0] == '\'' )
        { /* quoted argument */
          str_index = strchr (++farg, str_index[0] ); /* skip opening quote */
          if (! str_index)
            {
              fprintf
                (stderr,
                 "%s:%s:%d: unterminated string in configuration file\n",
                 CMDLINE_PARSER_PACKAGE, filename, line_num);
              result = EXIT_FAILURE;
              goto conf_failure;
            }
        }
      else
        { /* read up the remaining part up to a delimiter */
          next_token = strcspn (farg, " \t\r\n#\'\"");
          str_index += next_token;
        }

      /* truncate farg at the delimiter and store it for further check */
      delimiter = *str_index, *str_index++ = '\0';

      /* everything but comment is illegal at the end of line */
      if (delimiter != '\0' && delimiter != '#')
        {
          str_index += strspn(str_index, " \t\r\n");
          if (*str_index != '\0' && *str_index != '#')
            {
              fprintf
                (stderr,
                 "%s:%s:%d: malformed string in configuration file\n",
                 CMDLINE_PARSER_PACKAGE, filename, line_num);
              result = EXIT_FAILURE;
              goto conf_failure;
            }
        }

    noarg:
      ++my_argc;
      len = strlen(fopt);

      strcat (my_argv[my_argc], len > 1 ? "--" : "-");
      strcat (my_argv[my_argc], fopt);
      if (len > 1 && ((farg &&*farg) || equal))
            strcat (my_argv[my_argc], "=");
      if (farg && *farg)
          strcat (my_argv[my_argc], farg);
    } /* while */

  ++my_argc; /* for program name */
  my_argv_arg = (char **) malloc((my_argc+1) * sizeof(char *));
  for (i = 0; i < my_argc; ++i)
    my_argv_arg[i] = my_argv[i];
  my_argv_arg[my_argc] = 0;

  additional_error = (char *)malloc(strlen(filename) + strlen(ADDITIONAL_ERROR) + 1);
  strcpy (additional_error, ADDITIONAL_ERROR);
  strcat (additional_error, filename);
  result =
    cmdline_parser_internal (my_argc, my_argv_arg, args_info, override, initialize, check_required, additional_error);

  free (additional_error);
  free (my_argv_arg);

conf_failure:
  if (file)
    fclose(file);
  return result;
}
